import type { FeatureManifest } from '../../shared/lib/manifest.ts';

export const manifest: FeatureManifest = {
  slug: 'wallet-core',
  born: '2026-05-21',
  purpose:
    "The wallet itself: passphrase prompt on first login, key generation via tapit-attest, encrypted snapshot persisted through storage, unlock prompt on return. Provides the active Wallet object to the rest of the app via context. This is the product — everything else exists to serve this.",
  touches: [
    'src/features/wallet-core/createWallet.ts',
    'src/features/wallet-core/unlockWallet.ts',
    'src/features/wallet-core/saveWallet.ts',
    'src/features/wallet-core/createIdentityAttestation.ts',
    'src/features/wallet-core/WalletProvider.tsx',
    'src/features/wallet-core/WalletContext.ts',
    'src/features/wallet-core/useTransportPublish.ts',
    'src/features/wallet-core/useAutoBackup.ts',
    'src/features/wallet-core/useAutoBackup.test.ts',
    'src/features/wallet-core/useWallet.ts',
    'src/features/wallet-core/PassphrasePrompt.tsx',
    'src/features/wallet-core/PassphraseCommitWarnings.tsx',
    'src/features/wallet-core/ImportNostrIdentityPrompt.tsx',
    'src/features/wallet-core/parseNostrPrivateKey.ts',
    'src/features/wallet-core/parseNostrPrivateKey.test.ts',
    'src/features/wallet-core/createWalletFromImport.ts',
    'src/features/wallet-core/UnlockPrompt.tsx',
    'src/features/wallet-core/IdentityCeremony.tsx',
    'src/features/wallet-core/SignOutEscape.tsx',
    'src/features/wallet-core/SecureWalletPrompt.tsx',
    'src/features/wallet-core/WalletSplash.tsx',
    'src/features/wallet-core/UpdateBanner.tsx',
    'src/features/wallet-core/useUpdateAvailable.ts',
    'src/features/wallet-core/checkForUpdate.ts',
    'src/features/wallet-core/checkForUpdate.test.ts',
    'src/features/wallet-core/HomeScreen.tsx',
    'src/features/wallet-core/GatedLeafSection.tsx',
    'src/features/wallet-core/MyVouchesSection.tsx',
    'src/features/wallet-core/RequestVouchesModal.tsx',
    'src/features/wallet-core/IdentityGateSections.tsx',
    'src/features/wallet-core/backupBanner.ts',
    'src/features/wallet-core/backupBanner.test.ts',
    'src/features/wallet-core/backupNudge.ts',
    'src/features/wallet-core/backupNudge.test.ts',
    'src/features/wallet-core/BackupNudgeBanner.tsx',
    'src/features/wallet-core/IdentityCard.tsx',
    'src/features/wallet-core/AttestationCard.tsx',
    'src/features/wallet-core/useIdleLock.ts',
    'src/features/wallet-core/RotateKeySection.tsx',
    'src/features/wallet-core/inboxEnvelopeHandler.ts',
    'src/features/wallet-core/AdoptExistingKeySection.tsx',
    'src/features/wallet-core/adoptExistingKey.ts',
    'src/features/wallet-core/adoptExistingKey.test.ts',
    'src/features/wallet-core/OrgIdentitySections.tsx',
    'src/features/wallet-core/FamilyIdentitySections.tsx',
    'src/features/wallet-core/FamilyTabBody.tsx',
    'src/features/wallet-core/useOpenMemberRosterControls.ts',
    'src/features/wallet-core/useInboxAccepts.ts',
    'src/features/wallet-core/useInboxRouting.tsx',
  ],
  depends_on: ['auth', 'storage'],
  pause_safe: false,
  removal_safe: false,
  monetizable: false,
  notes:
    'PassphraseCommitWarnings (2026-05-27) gates wallet creation behind a two-step acknowledgment on both Classic (PassphrasePrompt) and Fresh (FreshOnboarding PassphraseStep) surfaces — first a personal-and-memorable check protecting against the "password manager autogenerated a random string and the operator clicked through without saving it" failure mode, then an irrecoverable-consequence check stating the no-reset / no-support / no-email-recovery truth. The form-submit on each surface only validates field-match plus minimum length now; actual wallet creation (or in Fresh, advancement past the passphrase step) is gated behind the two acknowledgments. Per DESIGN.md §5, the wallet stays unlocked for the session once the passphrase has been entered. Re-prompt on a fresh browser session. v1 ships passphrase only — biometric/WebAuthn unlock is Phase 7+. Phase 8 Phase E4 cut 5 (2026-05-27) extracted the three inbox accept-helpers (acceptRecoveryShare, acceptMembership, acceptSelfMembership) from HomeScreen into the new useInboxAccepts hook so HomeScreen could absorb the peer-side vouch-witness routing state without crossing the 800-line hard limit. The hook is callsite-shape-identical to the inline helpers — same wallet/ownerId/anchorWorker/identity/holdings dependencies pulled from useWallet, same orgDeclaration parameter (computed by the caller via findOwnOrgDeclaration), same return shape consumed by routeInbox. The 3 helpers share a homogeneous shape (verify-via-feature-module, hold-and-anchor, save, refresh, dismiss matching inbox row) so they cluster naturally into one hook, and future inbox-route accept-helpers (e.g. roster-update-accept, charter-amendment-accept in Phase D) can land in useInboxAccepts without re-growing HomeScreen. Family-mode ratify-leg cut (2026-05-27) extracted the inbox-routed modal stack itself into useInboxRouting — the hook owns the per-action state (incomingForWitness / incomingForAbsorb / incomingForRecovery / incomingForVouch / incomingForFamilyRatify plus their event-id companions), the routeInbox dispatcher, and the JSX for every modal an inbox envelope can route to (CosignAsWitnessModal in incoming-mode, AbsorbCosignModal with envelopeId-wide dismiss-dedup, RecoveryResponderModal, VouchWitnessModal, FamilyRatifyModal). useInboxRouting wraps useInboxAccepts so the membership-receive / self-membership-receive / recovery-share-receive helpers stay colocated with the modal mounts that consume them. HomeScreen now receives { routeInbox, modals: inboxModals } and renders inboxModals next to the other modal mounts; the extraction dropped HomeScreen from 796 to 662 lines, restoring headroom for the family-mode arc to keep landing without re-extracting on every cut. Peer-rotation fix cut 2 (2026-06-15): RotateKeySection now broadcasts a signed key-succession announcement to every handshake peer right after rotating (best-effort via sendEnvelope; failures don\'t block the rotation), and inboxEnvelopeHandler gained a branch that verifies an arriving announcement (transport/peerSuccession.isVerifiedAnnouncement) and silently holds it so the peer key-alias resolver can later map the peer\'s new key to the person we know — never surfaced as an inbox row, junk dropped. The rotate confirmation copy was updated to say peers are now told (it previously called the announcement a future feature). Cut 3 (messaging resolution: file incoming under resolveCanonical + address resolveCurrent + merge split threads) is the remaining piece. One-super-Family-tab consolidation (2026-06-26, operator: "we have family in two different spots, combine into one super family tab, kill any redundant actions or angles"): family used to live in two tabs at once — the signed family-unit / household section (FamilyIdentitySections) sat on the Identity tab while the genealogy tree (family-tree) and friends-trees sat on the dedicated Family tab, two different family models sharing a word with no code link between them, so a person added in one never appeared in the other and the app asked you to declare family twice. This cut relocates FamilyIdentitySections out of the Identity tab into FamilyTabBody as a third sub-view, so the Family tab toggle is now "Your tree | Household | Friends\' trees" and ALL family lives in one place; the Identity tab is now purely you (identity card, memberships, presence). It is a pure UI move: HomeScreen still owns the familyUnits memo + peerNames + the StartFamilyModal/editFamily modal mounts and threads familyUnits/namesByPubkey/onStartFamily/onEditFamily into FamilyTabBody as props, so no signed-envelope format changed and no migration was needed. "Share my tree" is hidden on the Household sub-view because it shares the genealogy tree, a different model. The deeper de-dup — making the household member list REFERENCE family-tree person-nodes so a person is entered once and both views read the same node — is a deliberate format-changing follow-up cut, intentionally NOT done here.',
};
