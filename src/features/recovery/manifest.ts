import type { FeatureManifest } from '../../shared/lib/manifest.ts';

export const manifest: FeatureManifest = {
  slug: 'recovery',
  born: '2026-05-24',
  purpose:
    'Phase 5e — hyphal lattice + Shamir cascade recovery (MYCELIUM_NETWORK_SPEC §12). Cut 5e-iii-a ships this: the operator declares which peers from their handshake web they would trust to help recover this wallet on a new device, along with the (M, N) threshold the recovery ceremony will require. The cohort is recorded as a self-signed credential, anchored to Bitcoin alongside everything else. Share distribution + backup-format v2 lands in 5e-iii-b. The recovery ceremony itself (initiator + responder + recovery-succession event) lands in 5e-v / -vi / -vii. The cryptographic floor (GF(256) Shamir split + combine) shipped in tapit-attest at 5e-ii. The load-bearing constraint stays loud: the Shamir split is over the backup ENCRYPTION KEY, never the signing keypair. Circle-trust gating (2026-06-15, connection-trust-model CUT C — briefs/2026-06-15-connection-trust-model-friction-first.md): circleTrust.ts (pure + tested) classifies the chosen cohort helpers by their strongest handshake tier (cryptographic in-person / self-attested-met / online / unknown) and returns a verdict against a recommended in-person minimum (a third, at least one). CohortEditorModal renders a warning banner from circleTrustWarning: amber when the in-person mix is thin, red when ZERO helpers are verified in person — and the red case gates the Save button behind an explicit "I am certain who these helpers are" acknowledgment. This is the trust model\'s "charge the certainty cost only at the high-stakes cut" — everyday connections pay nothing; the recovery circle, where it is load-bearing, is where in-person verification is asked for or the weakness is surfaced in plain language. Self-attested "we met in person" counts as its own softer tier and deliberately does NOT satisfy the strong gate. Never blocks outright (operator decides); the weakest case just requires a conscious tick.',
  touches: [
    'src/features/recovery/createCohort.ts',
    'src/features/recovery/createCohort.test.ts',
    'src/features/recovery/CohortEditorModal.tsx',
    'src/features/recovery/circleTrust.ts',
    'src/features/recovery/circleTrust.test.ts',
    'src/features/recovery/lattice.ts',
    'src/features/recovery/LatticePanel.tsx',
    'src/features/recovery/createShares.ts',
    'src/features/recovery/createShares.test.ts',
    'src/features/recovery/DistributeSharesModal.tsx',
    'src/features/recovery/createRecoveryRequest.ts',
    'src/features/recovery/createRecoveryRequest.test.ts',
    'src/features/recovery/RecoveryResponderModal.tsx',
    'src/features/recovery/RecoveryInitiatorModal.tsx',
    'src/features/recovery/RecoveryConfigStep.tsx',
    'src/features/recovery/RecoveryAwaitingShares.tsx',
    'src/features/recovery/RecoveryNamingStep.tsx',
    'src/features/recovery/recoveryInitiatorTypes.ts',
    'src/features/recovery/RecoveryKeyImportModal.tsx',
    'src/features/recovery/RestoreFromFileModal.tsx',
    'src/features/recovery/createRecoverySuccession.ts',
    'src/features/recovery/createRecoverySuccession.test.ts',
    'src/features/recovery/sharedSecret.ts',
    'src/features/recovery/sharedSecret.test.ts',
    'src/features/recovery/secretTemplates.ts',
    'src/features/recovery/secretTemplates.test.ts',
    'src/features/recovery/secretLedger.ts',
    'src/features/recovery/secretLedger.test.ts',
    'src/features/recovery/secretLiteracy.ts',
    'src/features/recovery/secretLiteracy.test.ts',
    'src/features/recovery/SecretsLedgerList.tsx',
    'src/features/recovery/SecretDetail.tsx',
    'src/features/recovery/SecretsDashboard.tsx',
    'src/features/recovery/PeopleSecretsSection.tsx',
    'src/features/recovery/secretPiece.ts',
    'src/features/recovery/secretPiece.test.ts',
    'src/features/recovery/SecretPieceReceiveModal.tsx',
    'src/features/recovery/secretPieceHeartbeat.ts',
    'src/features/recovery/secretPieceHeartbeat.test.ts',
    'src/features/recovery/useSecretPieceHeartbeat.ts',
    'src/features/recovery/secretFreshness.test.ts',
  ],
  depends_on: ['wallet-core', 'anchoring', 'connections', 'qr', 'transport'],
  pause_safe: true,
  removal_safe: true,
  monetizable: false,
  notes:
    'RecoveryInitiatorModal extraction 2026-05-28 (PLAN.md Tier 1 item 2): the three phase-render blocks (configuring → RecoveryConfigStep, sending/awaiting/combining/restoring → RecoveryAwaitingShares, naming → RecoveryNamingStep) plus shared types + helpers (recoveryInitiatorTypes) moved into sibling files so the orchestrator stays under the 800-line hard limit. Sub-components own presentation only; the absorb-share / publish-result / combine-and-restore / save-under-new-passphrase logic stays in the parent. Done pre-emptively before the Tier 1 cross-device recovery field-test so any fixes that test surfaces land in the right sub-component rather than re-growing the orchestrator. The cohort credential carries the member list as canonical JSON of {pubkey, name} pairs sorted + deduplicated so the digest is stable regardless of pick order. Latest-by-issuedAt wins for the in-effect cohort; older cohorts stay held + anchored for governance audit. Cohort members are sourced from existing handshakes today; organizations from memberships are a follow-on (would extend candidate pool from isMembership holdings). The publishCohort builder validates threshold + totalShares + member count + Shamir GF(256) ceiling (255 max) before signing. Family safe-word cut 2026-06-03: sharedSecret.ts reuses the same chassis Shamir split/combine the cohort uses but points it at an arbitrary secret STRING instead of the backup key — a self-contained, wallet-independent tool (split a word M-of-N, hand out the encoded pieces, any M paste back to rebuild). A fixed magic marker is prefixed before splitting so combining a wrong or insufficient set fails loudly rather than returning Shamir garbage that looks recovered. SharedSecretModal is the create/recover UI, SharedSecretSection is the launcher card mounted in IdentityGateSections (the "your people" grouping). Seamless-nostr cut 2026-06-04 ("make it more seamless and nostr"): each made piece can be sent straight to a person in the operator circle over the existing NIP-17 chat transport via sendChatMessage — the piece lands privately in that holder\'s Tapit wallet as a plain encrypted DM. Deliberately the LIGHTEST nostr option of the three offered (operator chose "DM pieces as chat"): the piece is a chat message, NOT a held/anchored recovery-share attestation, so there is no ceremony, no envelope kind, no inbox routing, and no permanent record on the holder side. The circle picker reuses findVouchingCircleCandidates (family/cohort/handshake); the per-piece "Send" affordance only appears when the Mycelium network is on (relayStatus !== null) and the operator has at least one connection. Rebuild stays the manual paste-back flow; Copy + QR remain the fallback for non-Tapit holders. This pulled useWallet + the circle-finder into the lazy modal chunk (budget bumped 3.5KB -> 5KB, audited). depends_on still adds nothing because the safe-word holds no wallet state of its own; it sits in recovery because it is the same Shamir substrate. "Cut version 1" 2026-06-04: generalized the single-purpose "Family safe word" into the "Your secrets" experience layer — secretTemplates.ts holds plain-language scenario presets (safe word, shared password, recover-for-me, break-glass, custom) and SharedSecretModal now leads with a scenario picker (pick -> create -> recover) so the operator configures by plain scenario, not by reasoning about thresholds, with all crypto jargon kept off the screen. v1 templates are all co-access over the existing Shamir core; blind-custody, timelocks, beneficiaries, and Bitcoin/Lightning payloads are deferred "more later" cuts captured in the 2026-06-04 conditional-release brief. Distribution ledger 2026-06-05 (field-test gap "no way to track where or why you sent secrets"): secretLedger.ts holds the pure record helpers (newSecretRecord / assignPiece / setWhy / upsert / remove), persisted encrypted-at-rest by storage/secretsLedgerStore.ts (mirrors messagesStore: encrypt/decrypt over idb, decrypt-failure returns []). The modal now defaults to a managed list landing (SecretsLedgerList) and a per-secret detail view (SecretDetail) showing who holds which piece by what method and when, plus an editable why-note and delete. Making a secret creates a record; chat sends auto-tag the holder, copy/QR get a "Tag" control. PRIME-DIRECTIVE LINE: the ledger stores METADATA ONLY BY DEFAULT — the secret value and share tokens are not persisted unless the operator explicitly opts in (see the re-sendable-pieces cut below). "Secrets condo in People" 2026-06-05 (operator: "secrets needs its own tab inside People where the messages are... an interactive dashboard condo, all the features right there, integrated not bolted on, with all the options to send over Nostr, QR, AirDrop, tap whatever"): the SharedSecretModal body was extracted into SecretsDashboard.tsx — the same list/pick/create/recover/detail flow rendered INLINE with no modal chrome, no body-scroll lock, no Close button. PeopleSecretsSection.tsx is the collapsible card that hosts it in the People tab (wallet-core PeopleTabBody, mounted right under the inbox); List + Tree stay visible always and the secrets card collapses down / expands when needed (operator chose that layout in chip form). The old Identity-tab launcher (SharedSecretSection) and the modal (SharedSecretModal) were deleted — one integrated home, not two. Share channels: the distribute view now offers Share over the Web Share API (shareText from shared/lib/share.ts → the iOS/PWA system share sheet: AirDrop, Messages, Mail, anything installed) alongside the existing chat-send (Nostr NIP-17), Copy, and QR. The Share button only renders when canShare() is true (navigator.share exists) so it does not duplicate Copy on desktop engines that would just fall through to clipboard; true NFC "tap" is not implemented anywhere in the wallet, so the system share sheet is the closest real channel and there is no fake tap affordance. Share/Copy/QR still require Tag to record the holder (the sheet is opaque about the recipient); only chat-send auto-tags. Re-sendable-pieces cut 2026-06-06 (operator reversed the earlier metadata-only-only choice — "rethink, the pieces can be hidden behind your passphrase"): SecretRecord.tokens (default ABSENT) optionally keeps a copy of the share tokens, set via setTokens() ONLY when the operator ticks "Keep a copy on this device so I can re-send these" in the make view (default off). Honest tradeoff surfaced at the toggle and in SecretDetail, never silent: with all tokens kept, this device + passphrase can reconstruct the secret, so the strongest setup leaves it off. When kept, SecretDetail shows a Re-send section (Copy/QR per piece) + a Stop-keeping-a-copy control; tokens ride the same passphrase-encrypted ledger store. This is Part A of briefs/2026-06-06-resendable-pieces-and-held-receipts-spec; Part B (held/acknowledged pieces + heartbeat-cessation liveness) is the deferred follow-on. Per-piece hashes 2026-06-06 (attack-list #2): SecretRecord.hashes keeps the SHA-256 of each piece (index-aligned), stored at make time REGARDLESS of keep-a-copy because a hash of a share is safe metadata that reveals nothing. pieceIndexForToken verifies a returned piece by hash without rebuilding the secret; SecretDetail surfaces a "Check a returned piece" tool. Cheap enabler for the retrieval/swap arc (Part C) and hardens recovery diagnostics. Teaching layer 2026-06-06 (attack-list #1): secretLiteracy.ts (explainThreshold live consequence + LEAK_VS_LOSS gut-check) wired into the SecretsDashboard create flow, jargon-guarded. Held/recognized pieces 2026-06-13 (attack-list B-1, "the full loop"): secretPiece.ts adds a structured `secret-piece` credential (token NIP-44 encrypted to the holder, mirrors recovery-share) + a `secret-piece-receipt` (holder->owner, status held/declined). The owner SEND path (SecretsDashboard.sendPiece) now sends the structured envelope via sendEnvelope instead of a plain chat blob; the holder routes through `secret-piece-receive` (envelopeRoute + peerCopy.secretPieceIncoming) to SecretPieceReceiveModal (keep -> holdSecretPiece + send a held receipt; let-go -> send a declined receipt). The receipt is recorded SILENTLY on arrival in inboxEnvelopeHandler (no inbox row, zero owner chore) via recordPieceReceipt into the secrets ledger, so SecretDetail shows each piece as confirmed/let-go. This is the foundation the heartbeat (B-2), handshake holding-slot (Part D), and recall brake (Part D-SECURITY) build on. depends_on gained transport (publishStatus). Heartbeat / liveness 2026-06-13 (attack-list B-2, the "Lovelines check"): a heartbeat is just another "held" receipt, so the owner already auto-collects it via B-1 — B-2 adds the holder re-send + the owner freshness read. secretPieceHeartbeat.ts has the pure due-selector (duePieces: held pieces never confirmed or older than HEARTBEAT_INTERVAL_MS ~30d) + runHeartbeat (re-sign a fresh held receipt per due piece, send to owner). useSecretPieceHeartbeat.ts fires it ONCE per session when Mycelium is live (mounted in HomeScreen), throttled by storage/heartbeatStore.ts (local, non-encrypted pieceKey->lastSent timestamps — not secrets). Owner side: secretLedger.pieceFreshness (fresh <=5wk / watch <=10wk / cold) + confirmedSummary (fresh confirmed holders vs threshold => margin); SecretDetail shows a readiness banner (margin >=2 green / 1 amber / <=0 red) only when confirmed>0 (copy/QR-only secrets have no liveness signal). Cessation is the signal: a dark/wiped holder simply stops pinging; never a false "deleted". Deferred: the one-tap nudge + the heartbeat hash-chain hardening + the recall brake (Part D-SECURITY).',
};
