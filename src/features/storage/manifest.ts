import type { FeatureManifest } from '../../shared/lib/manifest.ts';

export const manifest: FeatureManifest = {
  slug: 'storage',
  born: '2026-05-21',
  purpose:
    'Persistence layer for the encrypted wallet snapshot. Stores ciphertext in IndexedDB (offline-fast) and mirrors it to Supabase wallet_blobs (cross-device, recovery). The host only ever sees ciphertext; decryption happens client-side.',
  touches: [
    'src/features/storage/walletStore.ts',
    'src/features/storage/localStore.ts',
    'src/features/storage/remoteStore.ts',
    'src/features/storage/prefsStore.ts',
    'src/features/storage/mediaStore.ts',
    'src/features/storage/remoteMediaStore.ts',
    'src/features/storage/messagesStore.ts',
    'src/features/storage/dismissedInboxStore.ts',
    'src/features/storage/dismissedInboxStore.test.ts',
    'src/features/storage/inboxEnvelopeStore.ts',
    'src/features/storage/dismissedRequestsStore.ts',
    'src/features/storage/processedChannelEventsStore.ts',
    'src/features/storage/sharedNotesStore.ts',
    'src/features/storage/sharedNotesStore.test.ts',
    'src/features/storage/secretsLedgerStore.ts',
    'src/features/storage/foreignTreesStore.ts',
    'src/features/storage/heartbeatStore.ts',
    'src/features/storage/requestHistoryStore.ts',
    'src/features/storage/requestHistoryStore.test.ts',
    'src/features/storage/dismissedRequestsStore.test.ts',
    'src/features/storage/remoteRequestStateStore.ts',
    'supabase/migrations/20260521000001_create_wallet_blobs.sql',
    'supabase/migrations/20260522000001_create_wallet_media_bucket.sql',
    'supabase/migrations/20260814000001_create_request_state_backups.sql',
  ],
  depends_on: [],
  pause_safe: false,
  removal_safe: false,
  monetizable: false,
  notes:
    'Last-write-wins between local and remote, keyed by updated_at. Per DISCOVERY.md, the wallet_blobs table is scoped by RLS to the owner. Prefs gained a `lastLocalSync` ISO timestamp field 2026-05-28 (PLAN.md Tier 1 item 6) that always advances when walletStore.save completes locally, regardless of remote outcome — the home-screen backupBanner uses lastLocalSync > lastRemoteSync as the honest signal for "local changes have not reached the cloud yet" so the operator can see the unsynced state rather than have it lurk silently. Migration-safe: prefsStore.load merges defaults under saved prefs so pre-2026-05-28 wallets without the field inherit null and the new banner branch stays inactive until the next save advances it. Hardening 2026-05-31: prefs also gained `lastRemoteFailedSync` — walletStore.save sets it on the remote-catch branch and clears it to null on a successful remote write, so a persistent (e.g. multi-day) Supabase rejection becomes a sticky flag that survives reload instead of lurking behind only the soft local-newer note plus the day-late staleness banner. The home-screen backupBanner reads it as the highest-priority warn case under cloudSync-off and paints a red Retry banner whose button re-runs a full save to re-attempt the push. dismissedInboxStore (2026-05-31) persists the set of inbox envelopeIds the operator has permanently dismissed, per owner, keyed by stable envelopeId (NOT the per-relay eventId, which changes on every rebroadcast). The in-memory dismiss only cleared the row for the session; the Nostr relay re-delivers stored events on every unlock, so an envelope the wallet holds NO copy of — e.g. a counter-signed handshake whose peer wallet was deleted — reappeared forever as an "Absorb signature" row that Dismiss could not silence (operator field-test 2026-05-31). WalletProvider loads the set into a ref on owner change, the inbox handler consults it synchronously to drop replays on arrival, and dismissInboxEnvelope persists the envelopeId + clears every shown row sharing it. Not encrypted — envelopeIds are public content hashes, not secrets. inboxEnvelopeStore (2026-08-10) closes the gap dismissedInboxStore left open: it durably stores the FULL InboxEnvelope array (envelope content, sender, receivedAt, eventId), encrypted at rest with the wallet passphrase exactly like messagesStore does for chat. Before this, inboxEnvelopes lived only in WalletProvider React state and relied entirely on the relay re-delivering backlog on every unlock — a public relay\'s retention window is not this wallet\'s to depend on. Loaded by useInboxEnvelopePersistence (wallet-core feature) on unlock, filtered against dismissedRef so a dismissed item can never resurrect from a stale saved copy, and debounce-saved on every change once the load has resolved. dismissedRequestsStore (2026-08-11, operator: "Even after doing the work the tab acts like its first time") is dismissedInboxStore\'s sibling for the two REQUEST channels (psbt-cosign, vault-membership) instead of the generic envelope channel -- neither usePsbtCosignRequests.ts nor useVaultMembershipRequests.ts (sign-request feature) had ever persisted "already answered" before this, so a relay replay or a plain remount made an already-reviewed spend request or an already-accepted vault-membership offer look untouched again. Namespaced (\'psbt-cosign\' | \'vault-membership\') so both channels share one small module without colliding keys; psbt-cosign keys by event id (a relay resend of the exact same request keeps the same id), vault-membership keys by `${vault_descriptor}::${role}` instead, since a fresh request for the same offer can arrive under a brand new event id and still needs to count as already answered. Not encrypted, same reasoning as dismissedInboxStore -- these are request identifiers and vault descriptors, not secrets. processedChannelEventsStore (2026-08-11, operator: "I think it\'s the old messages coming into tapit again and again") stops psbtCosignChannel.ts / vaultMembershipChannel.ts from re-attempting verify/decrypt/parse -- and re-logging to channelDiagnostics, capped at 30 entries -- on an event already known to fail. Both channels subscribe with no `since` cutoff on purpose (a key rotation needs the full history re-checked), so a relay re-serves its whole matching backlog on every fresh subscribe; a permanently-undecryptable event (addressed to a since-rotated-away key) failed identically, over and over, burying genuinely new diagnostic entries behind old repeats. Keyed by wallet identity (stable across rotation) + channel + event id, deliberately covers ONLY the three failure stages (verify_failed, decrypt_failed, schema_failed) -- every one is a deterministic function of the event\'s immutable signed bytes, so "failed once" means "will fail exactly the same way forever." Never covers the success ("delivered") case: whether a successfully-decrypted request should still show depends on mutable session state (has it been accepted/declined yet?) that dismissedRequestsStore/vaultTrail.ts already handle correctly -- skipping reprocessing of a delivered event here would make a genuinely still-pending request vanish forever after one app restart, a worse bug than the noise this fixes. Not encrypted, same reasoning as dismissedRequestsStore. requestHistoryStore (2026-08-11, same session, operator: "still not showing past things in the inbox") is dismissedRequestsStore\'s sibling for keeping CONTENT, not just a dismissed-key -- Messages (messagesStore) and Family & circle (inboxEnvelopeStore) already kept full content "till you delete them" per the operator\'s own 2026-08-10 standard (see inboxEnvelopeStore\'s note above), but spend requests and vault-membership requests never did: dismiss() only ever remembered a bare key to stop an item resurfacing as pending, and threw the actual request away (who, which vault, when) the instant it was handled, with no way to ever see it again. requestHistoryStore closes that gap the same way, namespaced (\'psbt-cosign\' | \'vault-membership\') like dismissedRequestsStore, storing an array of {id, summary, detail, receivedAt, status, respondedAt} per (owner, namespace), newest-first, capped at 200 entries, upsertable by id so a status transition (pending -> reviewed/accepted/declined) replaces the row instead of duplicating it. Not encrypted, same reasoning as dismissedRequestsStore -- this is the same non-secret request metadata already shown in plaintext on the live pending banner. Removal is the only thing that permanently deletes a row (InboxScreen.tsx\'s new RequestHistoryList Delete button), matching the "till you delete them" principle exactly. remoteRequestStateStore (2026-08-14, operator, after a fresh sign-in flooded the Journal with ~13 already-handled DynastyTrust spend requests: "They are all old. App should know they are old and not show. I know it\'s because I resigned in. But we can anticipate it would happen again") is a generic encrypted key-value Supabase mirror, keyed by (owner_id, store_key), storing one opaque EncryptedBlob per row in the new request_state_backups table -- same shape as circlePhrase\'s remote mirror, generalized so any local-only store can back itself up without a bespoke table. dismissedRequestsStore and requestHistoryStore each grew push*/restore* functions on top of it (encrypt/decrypt via the wallet passphrase, same as inboxEnvelopeStore) so BOTH the dismissed-key set and the content history survive a wiped local IndexedDB -- which is exactly what a fresh sign-in does, and what re-adding a removed home-screen icon can also do. This was the actual root cause of the flood: local dismiss/history state is IndexedDB-only, with no durability across a device change or storage-partition wipe, so a relay backlog replay of old psbt-cosign/vault-membership events had nothing local to check against and every one looked brand new again. usePsbtCosignRequests.ts and useVaultMembershipRequests.ts (sign-request feature) now restore both stores from the cloud mirror on every unlock BEFORE subscribing to Nostr, and push both after every local mutation (new request received, dismiss, delete). restoreRequestHistoryBackup only pulls in remote entries whose id is not already present locally -- this session\'s own state is always treated as fresher than a cloud snapshot, so a restore can never resurrect an entry this device just deleted UNLESS the delete\'s own re-push has not yet landed (accepted best-effort gap on non-critical dismissal/history data, not a promise the tests claim). restoreDismissedRequestsBackup is a pure set-union, since a dismissal is monotonic (once dismissed, always dismissed) and can never be un-dismissed by a stale remote snapshot. Same "host only ever stores ciphertext" posture as every other cloud mirror in this app, even though the LOCAL copies of this specific data are already unencrypted by design (see dismissedRequestsStore/requestHistoryStore notes above) -- the architectural invariant is about what Supabase ever sees, not about local-only sensitivity. dismissedInboxStore load-order fix (2026-08-15, operator: "never got rid of the messages... spamming Home Screen every time you open app") -- this store\'s own note above ("WalletProvider loads the set into a ref on owner change, the inbox handler consults it synchronously") described the INTENT but not a guarantee: that load ran in an effect fully independent of both the transport-opening effect that consults the ref live and useInboxEnvelopePersistence.ts\'s (wallet-core feature) own restore-filter, with no ordering between any of them -- the identical "two independent effects, no ordering" defect already found and fixed for dismissedRequestsStore\'s two channels on 2026-08-11/14, just never applied to this, the original store the later ones were modeled on. Both real consumers now load this store themselves and await it before doing anything dependent on it, rather than trusting a ref some other effect may or may not have populated yet -- see wallet-core/manifest.ts for the consumer-side detail.',
};
