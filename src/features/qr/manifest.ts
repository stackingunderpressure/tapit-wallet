import type { FeatureManifest } from '../../shared/lib/manifest.ts';

export const manifest: FeatureManifest = {
  slug: 'qr',
  born: '2026-05-21',
  purpose:
    "Operator's punch list — QR encode for handing off envelopes and proofs across a room, QR decode via the device camera for receiving them. Encode uses the qrcode npm package (~10KB gzipped) and renders SVG so it scales clean on retina. Decode uses jsQR (pure-JS, canvas-pixel based) so it works identically on every browser getUserMedia works on, including Safari/iPhone.",
  touches: [
    'src/features/qr/QrShow.tsx',
    'src/features/qr/QrScanModal.tsx',
    'src/features/qr/encodeQr.ts',
    'src/features/qr/qrDecode.ts',
    'src/features/qr/ScanEnvelopeModal.tsx',
  ],
  depends_on: [],
  pause_safe: false,
  removal_safe: false,
  monetizable: false,
  notes:
    "Single-frame QR with a size guard — if the payload exceeds what fits in one QR code (roughly 2KB binary or 4KB alphanumeric at low error correction), the encoder throws and the UI surfaces a friendly message pointing at Share or Copy. Multi-frame animated QR is later polish. 2026-08-10 (operator: \"Dynasty trust camera works for qr scan flow. Tap it should too\"): decode switched from the native BarcodeDetector API to jsQR. The original claim that BarcodeDetector is 'available on iPhone Safari 17+' (this manifest's own words, born 2026-05-21) was never actually true -- WebKit has never shipped the Shape Detection API -- so QrScanModal's camera path silently fell into its unsupported-browser fallback on every iPhone, and the fallback's own 'pick a photo' escape hatch was ALSO gated on the same missing API, leaving only manual clipboard/text paste. DynastyTrust's own camera scanner (a separate repo) has always used jsQR and works reliably on the operator's iPhone; qrDecode.ts ports that same proven approach here. The isIosPwaStandalone() special case that defaulted QrScanModal to paste-only in installed-PWA mode is also removed -- it was compensating for the same root cause (framed as a PWA-standalone quirk when the actual cause was WebKit lacking BarcodeDetector everywhere, standalone or not), which jsQR does not have. (2026-08-13, operator report: camera scanning \"not getting proper permission\" when Tapit is opened from the iPhone home screen, confirmed as installed-standalone mode) -- that removal was wrong, and this is the correction. isIosPwaStandalone() detected two DIFFERENT problems and the 2026-08-10 fix only actually addressed one: BarcodeDetector's absence (a JS-decoder-level gap, real, fixed by jsQR, present in every WebKit context) is not the same thing as live getUserMedia being unreliable specifically in an installed iOS PWA's standalone browsing context (an OS/WebKit-level limitation of that context, unaffected by which library decodes the resulting frames) -- CameraCaptureModal.tsx's own header comment already named this second, separate problem correctly and never claimed jsQR would fix it. isIosPwaStandalone() moved to shared/lib/platform.ts (both features need it; one copy, not two) and QrScanModal now checks it on mount: standalone iOS skips attempting getUserMedia entirely (a prompt that may never fire, or a stream that silently never arrives, is worse than not trying) and lands on a new 'standalone' state explaining why in plain language, sharing the same paste/photo-capture UI the 'error' state already used. The photo-picker input also gained `capture=\"environment\"`, so \"Take a photo of the QR\" now opens the native camera directly (the same reliable native-capture path CameraCaptureModal already used) instead of only the photo library -- a strict improvement over the pre-08-10 paste-only default, not a reversion to it. KnownLimitationsSection.tsx in Settings already documented the two-alternatives behavior this restores; that copy had gone stale (describing behavior the 08-10 commit had silently removed) and is accurate again now. (2026-08-13, same day, operator: \"You have to do the barcode scanner not the camera\") -- reverted the standalone-skip-to-photo-capture behavior just described above; a single still photo is not an acceptable substitute for live point-and-scan on ANY platform, including an installed iOS PWA. isIosPwaStandalone() and the photo-capture path (fileInputRef, pickImage, the capture=\"environment\" input, the \"Take a photo of the QR\" button) are removed from QrScanModal entirely -- the live scanner is now ALWAYS attempted first regardless of platform, exactly like before the same-day standalone fix, and a getUserMedia failure falls through only to clipboard/manual paste, never to a photo decode. isIosPwaStandalone() itself stays in shared/lib/platform.ts and CameraCaptureModal.tsx still uses it (there a still photo genuinely IS the intended outcome, an unrelated feature) -- this reversal is scoped to QrScanModal only, so `qr`'s own touches list no longer includes shared/lib/platform.ts. KnownLimitationsSection.tsx's copy corrected again to describe the always-attempt-live / paste-on-failure behavior, no photo-capture mention.",
};
