import type { FeatureManifest } from '../../shared/lib/manifest.ts';

export const manifest: FeatureManifest = {
  slug: 'qr',
  born: '2026-05-21',
  purpose:
    "Operator's punch list — QR encode for handing off envelopes and proofs across a room, QR decode via the device camera for receiving them. Encode uses the qrcode npm package (~10KB gzipped) and renders SVG so it scales clean on retina. Decode uses jsQR (pure-JS, canvas-pixel based) so it works identically on every browser getUserMedia works on, including Safari/iPhone.",
  touches: [
    'src/features/qr/QrShow.tsx',
    'src/features/qr/QrScanModal.tsx',
    'src/features/qr/encodeQr.ts',
    'src/features/qr/qrDecode.ts',
    'src/features/qr/ScanEnvelopeModal.tsx',
    'public/manifest.webmanifest',
  ],
  depends_on: [],
  pause_safe: false,
  removal_safe: false,
  monetizable: false,
  notes:
    "Single-frame QR with a size guard — if the payload exceeds what fits in one QR code (roughly 2KB binary or 4KB alphanumeric at low error correction), the encoder throws and the UI surfaces a friendly message pointing at Share or Copy. Multi-frame animated QR is later polish. 2026-08-10 (operator: \"Dynasty trust camera works for qr scan flow. Tap it should too\"): decode switched from the native BarcodeDetector API to jsQR. The original claim that BarcodeDetector is 'available on iPhone Safari 17+' (this manifest's own words, born 2026-05-21) was never actually true -- WebKit has never shipped the Shape Detection API -- so QrScanModal's camera path silently fell into its unsupported-browser fallback on every iPhone, and the fallback's own 'pick a photo' escape hatch was ALSO gated on the same missing API, leaving only manual clipboard/text paste. DynastyTrust's own camera scanner (a separate repo) has always used jsQR and works reliably on the operator's iPhone; qrDecode.ts ports that same proven approach here. The isIosPwaStandalone() special case that defaulted QrScanModal to paste-only in installed-PWA mode is also removed -- it was compensating for the same root cause (framed as a PWA-standalone quirk when the actual cause was WebKit lacking BarcodeDetector everywhere, standalone or not), which jsQR does not have. (2026-08-13, operator report: camera scanning \"not getting proper permission\" when Tapit is opened from the iPhone home screen, confirmed as installed-standalone mode) -- that removal was wrong, and this is the correction. isIosPwaStandalone() detected two DIFFERENT problems and the 2026-08-10 fix only actually addressed one: BarcodeDetector's absence (a JS-decoder-level gap, real, fixed by jsQR, present in every WebKit context) is not the same thing as live getUserMedia being unreliable specifically in an installed iOS PWA's standalone browsing context (an OS/WebKit-level limitation of that context, unaffected by which library decodes the resulting frames) -- CameraCaptureModal.tsx's own header comment already named this second, separate problem correctly and never claimed jsQR would fix it. isIosPwaStandalone() moved to shared/lib/platform.ts (both features need it; one copy, not two) and QrScanModal now checks it on mount: standalone iOS skips attempting getUserMedia entirely (a prompt that may never fire, or a stream that silently never arrives, is worse than not trying) and lands on a new 'standalone' state explaining why in plain language, sharing the same paste/photo-capture UI the 'error' state already used. The photo-picker input also gained `capture=\"environment\"`, so \"Take a photo of the QR\" now opens the native camera directly (the same reliable native-capture path CameraCaptureModal already used) instead of only the photo library -- a strict improvement over the pre-08-10 paste-only default, not a reversion to it. KnownLimitationsSection.tsx in Settings already documented the two-alternatives behavior this restores; that copy had gone stale (describing behavior the 08-10 commit had silently removed) and is accurate again now. (2026-08-13, same day, operator: \"You have to do the barcode scanner not the camera\") -- reverted the standalone-skip-to-photo-capture behavior just described above; a single still photo is not an acceptable substitute for live point-and-scan on ANY platform, including an installed iOS PWA. isIosPwaStandalone() and the photo-capture path (fileInputRef, pickImage, the capture=\"environment\" input, the \"Take a photo of the QR\" button) are removed from QrScanModal entirely -- the live scanner is now ALWAYS attempted first regardless of platform, exactly like before the same-day standalone fix, and a getUserMedia failure falls through only to clipboard/manual paste, never to a photo decode. isIosPwaStandalone() itself stays in shared/lib/platform.ts and CameraCaptureModal.tsx still uses it (there a still photo genuinely IS the intended outcome, an unrelated feature) -- this reversal is scoped to QrScanModal only, so `qr`'s own touches list no longer includes shared/lib/platform.ts. KnownLimitationsSection.tsx's copy corrected again to describe the always-attempt-live / paste-on-failure behavior, no photo-capture mention. (2026-08-14, operator: \"The qr scanner still not working on tap it wallet. Its black not scanning\") -- a real, different bug from any of the three fixes above, and this time not a platform/permission problem at all: every caller of QrScanModal (ScanEnvelopeModal, HandshakeModal, JoinOrgModal, MembershipModal, CosignAsWitnessModal, AbsorbCosignModal, VerifyProofScreen, RecoveryInitiatorModal) passes onScanned as a plain, unmemoized function, and the camera-acquisition effect listed onScanned in its dependency array. Any unrelated re-render in the parent tree while the camera was already live -- a WalletContext value ticking, a subscription firing, nothing to do with the scanner itself -- tore that effect down: every MediaStreamTrack got stopped, and the effect's own `state.kind !== 'starting'` guard then refused to restart it, since state had already moved to 'scanning'. A stopped track renders as a solid black frame, which is exactly what got reported. Fixed by routing onScanned through a ref (onScannedRef, kept current via its own small effect) so the camera effect only ever depends on state.kind -- once the stream is live, nothing but the modal actually closing tears it down again. This is unrelated to isIosPwaStandalone() or jsQR; those two prior fixes addressed real problems in getting the camera to START, this one addresses it silently dying after it already had. (2026-08-14, same day, operator confirmed the black screen persisted after the onScanned-ref fix above, then reported DynastyTrust's own camera scanner works fine on the same phone) -- traced this properly instead of guessing a fourth time. DynastyTrust's QrScanner.tsx turned out to be built almost identically to this one (same getUserMedia call, same playsInline+muted video, same requestAnimationFrame + jsQR loop) -- porting its code would have changed nothing, because the difference was never the JS. DynastyTrust ships no service worker and no web manifest at all, so even added to an iOS home screen its icon just opens a normal Safari tab. Tapit Wallet's own public/manifest.webmanifest declared \"display\": \"standalone\", which is what makes iOS launch it full-screen with no browser chrome from its home-screen icon -- and that specific standalone mode is the exact context CameraCaptureModal.tsx's header comment already named as unreliable for live getUserMedia. The operator confirmed no permission prompt ever appeared, matching that comment precisely, and confirmed DynastyTrust is also on the home screen and still works -- consistent, not contradictory, once the manifest-driven distinction is accounted for: a home-screen icon only launches standalone if the site's own manifest asks for it. Fixed at the root rather than inside this component: manifest.webmanifest's display changed from \"standalone\" to \"browser\", so Tapit now opens in ordinary Safari from its icon exactly like DynastyTrust already does reliably. Operator chose this over the alternative (keep standalone everywhere else, escape to Safari only for the scan step and bridge the result back) for simplicity and because it is the literal proven-working configuration, accepting the tradeoff that the whole app now shows ordinary Safari chrome instead of a full-screen native-app presentation. isIosPwaStandalone() (shared/lib/platform.ts) and CameraCaptureModal's fallback branch built on it are left in place, not deleted -- matchMedia('(display-mode: standalone)') will simply always read false going forward, so that code degrades to dead but harmless rather than needing to be torn out. One real caveat for the operator: iOS caches a home-screen icon's launch mode from whenever \"Add to Home Screen\" was originally tapped, so an icon added before this fix may need to be removed and re-added to actually pick up the new browser mode. (2026-08-15, operator: \"Camera still not working in tap it. Asked for permission but still black screen.\") -- a sixth occurrence of the same symptom, but this one was a genuine, deterministic, platform-independent bug hiding in plain sight the whole time, not another PWA/permissions edge case: the acquisition effect's dependency array was `[state.kind]`, and the effect's OWN success path called `setState({ kind: 'scanning' })` -- which changes state.kind, which is the effect's own dependency, which forces React to tear the effect down (cleanup stops every MediaStreamTrack) and run it again, where it immediately hits the `state.kind !== 'starting'` guard and does nothing. The camera starts, gets a live frame, transitions to 'scanning', and stops itself before ever painting -- on every single attempt, on every platform, independent of the standalone-vs-browser display-mode fix above. This is NOT the same bug the 2026-08-14 onScanned-ref fix addressed (that one was an external trigger -- a parent re-render -- tearing down an already-live stream); this one is the effect tearing down its OWN successful acquisition, and it was already present, unnoticed, underneath that fix and everything after it. Fixed by decoupling the retry trigger from the state machine: a new `attempt` counter (state) is the effect's only dependency, incremented only when something explicitly wants a fresh acquisition (mount for camera mode, the Try camera button), never by the effect's internal setState calls. The live stream/rafId/cancelled-flag moved into refs (streamRef/rafIdRef/cancelledRef) so a `stopCamera()` helper can explicitly tear the camera down when the user taps \"Or paste the text instead\" while scanning -- necessary because that transition no longer happens for free as a side effect of the old (buggy) state-dependency wiring. No new test added -- this repo has no component-rendering test infrastructure (no @testing-library/react), matching the precedent set by the 2026-08-14 fix, which also shipped without one.",
};
