import type { FeatureManifest } from '../../shared/lib/manifest.ts';

export const manifest: FeatureManifest = {
  slug: 'inbox',
  born: '2026-08-10',
  purpose:
    "One screen aggregating everything that has ever arrived at this wallet over Nostr -- chat messages, spend requests, vault-membership invites, family/circle attestation arrivals, and safety phrases -- with category tabs, per the operator's 2026-08-10 request: \"we need an inbox ... I feel like we need one spot where all of that is.\" Every category already had a receiving surface scattered elsewhere in the app (a chat list buried in the People tab, spend-request and vault-invite banners at the top of Home, safety phrases only visible in Settings); this feature does not re-implement any of that receive/accept/decline logic, it composes the existing self-contained hooks and components into one place reachable from a header link.",
  touches: ['src/features/inbox/InboxScreen.tsx'],
  depends_on: ['wallet-core', 'transport', 'sign-request', 'circle-phrase', 'connections'],
  pause_safe: true,
  removal_safe: true,
  monetizable: false,
  notes:
    "(2026-08-19, operator, after confirming a sign-in request had actually arrived and been signed via the Nostr-delivered pubkey-paste flow: \"it never showed up in the inbox as a message you could see later\") -- IncomingSignInBanner.tsx (sign-request feature, 2026-08-19) was built and mounted as a live banner on HomeScreen, and its backing hook useSignInRequests.ts already persisted every request to requestHistoryStore under its own 'sign-in' namespace exactly like the Spend requests and Vault invites hooks do -- but nothing in InboxScreen.tsx ever called useSignInRequests() or rendered its banner/history, so a reviewed sign-in request's history row existed in IndexedDB with no screen anywhere that read it back. Fixed by giving Sign-ins the identical treatment Spend requests and Vault invites already have: a new 'signins' category tab, one useSignInRequests() call alongside the other two per-category hooks (same single-fetch-passed-as-prop discipline the 2026-08-11 duplicate-subscription fix established -- InboxScreen renders IncomingSignInBannerView with that state, never the self-subscribing IncomingSignInBanner container HomeScreen already owns), and a RequestHistoryList section using the same reviewed-only status label PSBT_STATUS_LABEL already uses (a sign-in's dismiss() marks 'reviewed' the moment Review is tapped, same limitation Spend requests already has -- neither hook currently learns whether the user went on to actually approve or decline once inside SignApprovalScreen, so 'Reviewed' is the honest label, not 'Approved'). Confirmed NOT a bug: the operator's message also asked whether a vault should have shown up as \"joined\" -- it should not, because the pubkey-paste flow only ever sends a sign-in (authentication) request, never a vault-membership request; those are separate SignRequest intents with separate channels (vaultMembershipChannel.ts, kind 9578) and DynastyTrust's paste-pubkey connect modal only calls the sign-in delivery path (tapit-signin-request-delivery.ts). A vault join would need to arrive as its own, separate request. InboxScreen.tsx is read-only aggregation, not a new data path: it calls useWallet() for inboxEnvelopes/chatThreadsByPeer (already-global state owned by WalletProvider, no new subscription), mounts its own useInboxRouting() instance (same hook HomeScreen uses, independently instantiated -- each screen owns its own modal stack), and renders the EXISTING InboxPanel and CirclePhraseSection components as-is inside category sections. The 'Messages' tab lists chatThreadsByPeer (global chat state) sorted by last-message time and links to '/?tab=people', which HomeScreen.tsx's new initialTabFromUrl() reads once on mount to land directly on the People tab where the full thread lives -- deep-linking into a SPECIFIC peer's open thread was deliberately left alone (that selection state is owned locally inside PeopleTabBody and lifting it to the URL is a bigger, separate change not needed to satisfy 'one spot to see it's there and click through'). Duplicate-subscription fix (2026-08-11, operator: \"Just received a spend request but didn't show in inbox\") -- this note used to claim the screen's own usePsbtCosignRequests/useVaultMembershipRequests calls alongside IncomingPsbtCosignBanner/IncomingVaultMembershipBanner's internal calls to the SAME hooks was intentional, \"matching the existing pattern.\" It was wrong: two fully independent subscription instances, each running its own async load-persisted-set-then-subscribe chain, could disagree about what had arrived or about which had finished subscribing yet -- the empty-state text and the actual banner could show two different realities on the same screen. Fixed in the sign-request feature (IncomingPsbtCosignBannerView / IncomingVaultMembershipBannerView, presentational components taking already-fetched state as a prop); this screen now calls each hook exactly once and passes that single state to both the empty-state check and the *View component. \"Still not showing past things\" fix, same session -- Spend requests and Vault invites never got the \"keep until you delete it\" treatment Messages and Family & circle got on 2026-08-10 (requestHistoryStore.ts, storage feature): dismiss() only ever remembered a bare key, throwing the request's content away the instant it was handled. Both hooks now also return `history` (every request ever seen, newest first) and `deleteHistoryEntry`; this screen's new local RequestHistoryList component renders the HANDLED subset (status !== 'pending' -- a still-pending item already shows in the live banner above, showing it twice would be redundant) with a Delete button per row, reusing the same non-pending filter and status-label maps for both categories.",
};
