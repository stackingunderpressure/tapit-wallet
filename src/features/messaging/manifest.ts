import type { FeatureManifest } from '../../shared/lib/manifest.ts';

export const manifest: FeatureManifest = {
  slug: 'messaging',
  born: '2026-05-25',
  purpose:
    'Sub-cut 2b of the per-peer chat surface roadmap (brief 2026-05-24-per-peer-chat-surface-roadmap.md). The per-peer thread surface that opens under the People tab when the operator taps a ConnectionCard (Classic) or a Crew bubble (Fresh). Renders Tier 1 chat messages — NIP-17 gift-wrapped events (kind 1059) from the transport, signed and end-to-end encrypted, not anchored, not attestations — in iMessage-shape with the operator-aligned right and the peer-aligned left, a header carrying the peer name + relationship chip + Tier P/R badge, and a composer pinned at the bottom. Messages live in WalletContext per-peer (Map<peerPubkey, ThreadMessage[]>) backed by storage/messagesStore.ts via the useChatPersistence hook — load on first unlock per owner, debounce-save on every state change. Operator-reported bug fix 2026-05-25: outbound messages were vanishing on reload because the relay subscription filter is #p=mine, so the relay never re-delivered the operator own sent messages back. IDB persistence (local-only, encrypted at rest via tapit-attest PBKDF2-AES — same posture walletStore + mediaStore already use, passphrase flowed in through useChatPersistence) closes that gap. Cut 4 still owns the opt-in cloud-backup toggle (default OFF). Sub-cut 2c shipped 2026-05-25: promote-to-envelope plus-menu (composer + button) and bubble long-press both open a bottom-sheet PromoteMenu seeded with the source text. First target wired end-to-end is Save as journal entry — the chat moment opens JournalComposer with the source text in the body and the peer pinned as the About-someone-else subject; if the handshake carries relationship=family the category pre-picks Family. PROMOTE_TARGETS in promoteTarget.ts is data-driven so future targets (mark-presence, witness-ask, cosign-request, share-held-envelope, disclose-proof) plug into the same menu without restructuring.',
  touches: [
    'src/features/messaging/manifest.ts',
    'src/features/messaging/threadMessage.ts',
    'src/features/messaging/bubbleFormat.ts',
    'src/features/messaging/MessageBubble.tsx',
    'src/features/messaging/MessageComposer.tsx',
    'src/features/messaging/PeerThread.tsx',
    'src/features/messaging/useChatPersistence.ts',
    'src/features/messaging/useChatTransport.ts',
    'src/features/messaging/PromoteMenu.tsx',
    'src/features/messaging/promoteTarget.ts',
    'src/features/messaging/promoteToJournalPrefill.ts',
    'src/features/messaging/promoteToPresencePrefill.ts',
    'src/features/messaging/PromoteRouter.tsx',
    'src/features/messaging/HeldEnvelopePicker.tsx',
    'src/features/messaging/useLongPress.ts',
  ],
  depends_on: ['wallet-core', 'transport', 'connections', 'theme', 'presence', 'cosigning', 'disclosure'],
  pause_safe: true,
  removal_safe: false,
  monetizable: false,
  notes:
    'useChatTransport (2026-05-27 extraction) owns the per-peer thread map, the chat-kind subscription, the sendChatMessage callback, and the useChatPersistence wiring; WalletProvider hosts the hook and re-exposes chatThreadsByPeer + sendChatMessage through WalletContext so the messaging feature\'s consumer surface (useWallet().chatThreadsByPeer + sendChatMessage) is unchanged. WalletProvider also exposes purgePeerThread to its removePeerConnection callback so the chat-thread purge stays atomic with the handshake unhold. The transport state was lifted from a ref to React state so useChatTransport reacts to availability — the chat hook subscribes when the transport becomes non-null and tears down when it returns to null; WalletProvider\'s inbox-subscription effect still owns transport open/close lifecycle. The chat subscription previously ran nested inside WalletProvider\'s transport useEffect as a sibling to the inbox handler — same transport handle, separate handler, independent kind filter (NIP17_GIFT_WRAP_KIND = 1059 vs TAPIT_ENVELOPE_KIND = 9573). Outbound chat optimistically appends to the local thread before publish settles so the composer feels instant; if publish fails the message stays in the thread but a future polish cut will mark it as undelivered. ConnectionCard gains an optional onOpen prop; ClassicConnections + FreshCrew pass it through. removal_safe: false because HomeScreen imports PeerThread. NIP-17 chat migration (2026-05-26 evening, see transport manifest for the rationale): the wire format moved from custom kind 9574 to NIP-17 gift-wrapped kind 1059 because public relays were accept-but-not-persist on the custom kind, leaving recipients offline-then-online unable to retrieve messages. The migration kept the messaging feature\'s consumer surface (sendChatMessage callback + chatThreadsByPeer Map) unchanged — only the encryptedInbox.ts helpers underneath changed wire shape. Threads continue to track by the REAL peer pubkey (recovered from the seal\'s signed pubkey, not the gift wrap\'s ephemeral wrapper) so the per-peer Map and the existing IDB persistence layer work without migration. Privacy upgrade: the relay no longer sees the real sender pubkey on each chat publish. Peer-rotation fix cut 3 (2026-06-15, completes the audit fix): useChatTransport now takes holdings and builds a verified peer key-alias (transport/peerSuccession.buildPeerKeyAlias). Incoming chat is filed under resolveCanonical(senderPubkey) — so a peer who rotated lands in their EXISTING thread (keyed by their canonical/genesis key, which equals their handshake key, so PeerThread\'s lookup is unchanged for the common case), not a new invisible one. Outgoing messages address resolveCurrent(recipientPubkey) so a rotated peer is reached at their current key while thread state stays under the canonical key. The alias lives in a ref rebuilt as holdings change so the long-lived subscription + send callback always read the latest. With cut 1 (peerSuccession substrate) + cut 2 (announce-on-rotate + inbox ingest), messaging now follows a peer across a key rotation end to end. Edge case deferred: a peer who had ALREADY rotated before you handshook them (handshake key != genesis) — rare; PeerThread would then need to resolve its peerPubkey to canonical too.',
};
