import type { FeatureManifest } from '../../shared/lib/manifest.ts';

export const manifest: FeatureManifest = {
  slug: 'family-tree',
  born: '2026-06-14',
  purpose:
    'The sovereign family tree (CUT 1 foundation — the pure graph core). A family member is a PERSON NODE; most ancestors hold no wallet and are WITNESSED in by the living (keyless), while living members can be keyed to their own wallet. The tree is built from one primitive blood edge (parent_of) plus a spouse edge for affinity; every other relationship — grandparent, great^N-grandparent, sibling, aunt/uncle, niece/nephew, cousin Nth-removed, in-law / by-marriage — is DERIVED by walking the graph (relationshipLabel), which is the engine behind "it can name that it is third cousins." This first cut ships data substrate only: pure draft builders (buildPersonNodeDraft / buildParentEdgeDraft / buildSpouseEdgeDraft following the buildHandshakeDraft discipline), predicates + readers (isPersonNode / readPersonNode / isKinEdge / readKinEdge), and the pure graph reader + namer (buildKinGraph / relationshipLabel). No UI, no signing-pipeline wiring, no network yet — those follow in later slices, then the handshake-merge cut. Editor slice (2026-06-14, same day): createFamilyTree.ts adds the impure persistence layer (createPersonNode / createKinEdge = buildDraft -> wallet.sign -> wallet.hold -> anchorQueue.upsert, caller saves once, mirrors createJournalEntry / StartFamilyModal). FamilyTreeEditor.tsx is a lazy-loaded modal — the edit-your-adjacent-layer surface: it lists your people with their DERIVED relationship label (via buildKinGraph + relationshipLabel) and adds a parent / child / spouse / sibling. parent/child/spouse are one primitive edge from your own self-node (a person-node keyed to your identity, created lazily on first add); sibling attaches under your first existing parent (disabled with a hint until you have one). Optimistic local state shows adds immediately regardless of context-refresh timing. Mounted from PeopleTabBody behind an "Edit family tree" button in the Tree view, React.lazy so its chunk only loads on open. removal_safe is now false because PeopleTabBody imports the editor. Render slice (2026-06-15): kinGraph gained generationOf (genealogical generation: +older / 0 same / -younger, blood + affinity), and treeGenerations.ts groups the graph into generation rows (oldest at top, you in the middle, children below) with friendly titles + each member\'s derived relationship; the editor now renders the tree by generation instead of a flat list, so it reads as a shape. Pure + tested. Node-detail slice (2026-06-15): tapping a person opens their detail — relationship to you + the moments/stories you have signed about them (storiesAbout.ts matches journal attestations by subject: keyed pubkey for living members, display-name label for keyless ancestors like Pam; name-matching is honestly fragile). This is the Pam-node mockup made real, tying the memory keystone to the tree. depends_on gained journal (momentTimestamp for newest-first ordering). Hardening slice (2026-06-15): createJournalEntry gained an optional subjectNode input that writes a subject_node leaf (the person-node envelopeId); storiesAbout now matches that robust link FIRST and falls back to subject name/pubkey only for legacy entries. The node detail gained an inline "Add a moment about <person>" composer (text + optional honest event date) that signs a Family-category journal entry bound to the node via subject_node, holds + anchors + saves, and shows it optimistically — so a moment can be written from the person and binds to them, not just a name. Merge core (CUT 3 pure brain, 2026-06-15): kinEdge gained a same_as relation + buildSameAsEdgeDraft — the HUMAN-CONFIRMED binding that declares two person-nodes are one person (never auto-created on a name match). buildKinGraph now union-finds same_as edges so bound nodes fuse into ONE canonical node (deterministic canonical: keyed-preferred then smallest id), merging their detail and carrying every original id in aliasIds; parent/spouse edges remap onto canonical ids and self-loops drop. storiesAbout matches subject_node against aliasIds so a story bound to a pre-merge id still finds the canonical person. mergeCandidates.ts is the proposal brain: given my graph, a relative\'s graph, and the shared keyed person we both connect through, it surfaces keyless same-named relatives with compatible dates as candidates for a human to confirm — pure, no auto-merge. Still no UI/network — the proposal modal + handshake tree-exchange + scoped inheritance are the next slices. Gendered-naming slice (2026-06-16): person-nodes gained an optional sex (female/male, never invented); personNode stores/reads a sex leaf, kinGraph carries it onto the canonical node, and gender.ts turns the neutral structural label from relationshipLabel into the family\'s own word for that relative (mother/father, grandmother/grandfather, sister/brother, daughter/son, aunt/uncle, niece/nephew, wife/husband, the -in-law forms; cousins stay neutral, great-/grand- prefixes preserved). The editor add-form gained a relation-aware Mother/Father (Sister/Brother, …) toggle and shows the gendered label on the tree and the person detail, while kinEducation still keys off the neutral label. Operator field report 2026-06-16: could not distinguish mother from father or grandma from grandpa; this closes that. Connected-tree slice (2026-06-16, operator: "not a Merkle-looking tree"): the flat generation-row list is replaced by a real branching node-link diagram. treeLayout.ts is the pure layout engine — it layers nodes by generation (oldest on top, you in the middle, descendants below, untraceable in one bottom row) and orders columns by a barycenter sweep so children sit under their parents and lineage lines read cleanly; deterministic + unit-tested. FamilyTreeCanvas.tsx is the dumb SVG renderer (orthogonal parent_of connectors, dashed spouse links, you ringed, scrolls on a phone), tapping a node opens the same person detail. KinAvatar was extracted to its own file so the list and canvas share it. Visible-branches + ancestor-slots slice (2026-06-16, operator: "Draw the tree branches also. And missing known spots above you to fill in names."): the parent_of connectors were drawn at 0.18 opacity (near-invisible) — bumped to a clear warm-ink 0.55 at 2.25px so the tree actually reads as connected; ancestorSlots.ts (pure + tested) computes the missing "known spots above you" — every person on your direct ancestral spine (you + your parents, generationOf 0 and 1) has two biological parents, so any unfilled parent slot up to grandparents is surfaced as a synthetic placeholder; withAncestorSlots clones the graph and injects those placeholders as dashed parent nodes the canvas draws as "+ add parent" ghosts with faint dashed branches; tapping one primes the add form (slotTarget) to add a parent of that exact child. The canvas draws off a separate canvasGraph so people/generation counts stay measured on real nodes only. NOTE on the editing + ratification asks from the same message: full detail editing already worked (createPersonEdit writes append-only latest-wins corrections that fold into the optimistic graph, so a corrected birthdate overwrites the wrong one everywhere), and the all-kids-sign ratification BRAIN already exists in foldPersonEdits (solo controller edits at will; 2+ controlling signers => every edit needs all of them, shown via the requiresCosign banner + pending/signed badges). The one unbuilt half is the OVER-THE-NETWORK collection of siblings co-signatures on a proposed person-edit (mirrors FamilyRatifyModal\'s Mycelium round-trip for family units) — named as the next dedicated cut. Same-as persistence extension (2026-06-16, friends-trees consented-merge slice): createKinEdge now accepts the \'same_as\' relation (in addition to \'parent_of\' | \'spouse\'), routing to buildSameAsEdgeDraft through the same sign -> hold -> anchorQueue.upsert pipeline so the friends-trees view confirms a "people you both know" candidate as one person via the wallet\'s standard persistence path rather than a bespoke one; the same_as edge carries only the two node ids + the author signature (no personal data). The confirm UI + the pure "is this pair already linked" reader live in the friends-trees feature (FriendTreesView.tsx) and sameAsLinks.ts respectively.',
  touches: [
    'src/features/family-tree/personNode.ts',
    'src/features/family-tree/personNode.test.ts',
    'src/features/family-tree/kinEdge.ts',
    'src/features/family-tree/kinEdge.test.ts',
    'src/features/family-tree/kinGraph.ts',
    'src/features/family-tree/kinGraph.test.ts',
    'src/features/family-tree/gender.ts',
    'src/features/family-tree/gender.test.ts',
    'src/features/family-tree/personEdit.ts',
    'src/features/family-tree/personEdit.test.ts',
    'src/features/family-tree/treeLayout.ts',
    'src/features/family-tree/treeLayout.test.ts',
    'src/features/family-tree/ancestorSlots.ts',
    'src/features/family-tree/ancestorSlots.test.ts',
    'src/features/family-tree/FamilyTreeCanvas.tsx',
    'src/features/family-tree/FamilyTreeExplorer.tsx',
    'src/features/family-tree/TreeCanvasArea.tsx',
    'src/features/family-tree/TreeViewToggle.tsx',
    'src/features/family-tree/focusNeighbors.ts',
    'src/features/family-tree/focusNeighbors.test.ts',
    'src/features/family-tree/PersonDetailView.tsx',
    'src/features/family-tree/KinAvatar.tsx',
    'src/features/family-tree/createFamilyTree.ts',
    'src/features/family-tree/treeGenerations.ts',
    'src/features/family-tree/treeGenerations.test.ts',
    'src/features/family-tree/storiesAbout.ts',
    'src/features/family-tree/storiesAbout.test.ts',
    'src/features/family-tree/mergeCandidates.ts',
    'src/features/family-tree/mergeCandidates.test.ts',
    'src/features/family-tree/FamilyTreeEditor.tsx',
    'src/features/friends-trees/FriendTreesView.tsx',
    'src/features/family-tree/kinEducation.ts',
    'src/features/family-tree/kinEducation.test.ts',
    'src/features/wallet-core/PeopleTabBody.tsx',
    'scripts/bundle-budget.mjs',
  ],
  depends_on: ['wallet-core', 'anchoring', 'theme', 'journal'],
  pause_safe: true,
  removal_safe: false,
  monetizable: false,
  notes:
    'Spec: briefs/2026-06-14-editable-family-tree-and-handshake-merge-spec.md. Honesty boundary (same as the journal Moments cut): born/died are CLAIMS recorded now; the signing date is never forged, and keyed_pubkey is set ONLY when the person genuinely has a wallet (never invented; a keyless child node becomes keyed via the custody-handoff pattern when they get their own wallet). A person-node is a credential-kind attestation (credential_type=person_node); a kin edge is a relationship-kind attestation (kin_relation=parent_of|spouse, kin_from/kin_to referencing person-node envelopeIds). The node id is envelopeId(signedAnchor) — the same content-address every other feature uses. Nodes + edges are family-co-signable later so the canonical node accretes weight; the merge cut (CUT 3) binds duplicate nodes across two relatives\' trees, anchored on the shared keyed person and human-confirmed (never silent auto-merge on name). relationshipLabel handles consanguinity via most-recent-common-ancestor (cousin number = min(depths)-1, removed = |depth difference|) and one-hop affinity (spouse of a blood relative, or a blood relative of one\'s spouse); deeper affinity chains are a follow-on. Pure + fully unit-tested; pause_safe and removal_safe because nothing yet imports it (substrate only). depends_on wallet-core for the tapit-attest builders/types. Follow-on slices: the edit-your-adjacent-layer editor UI, PeopleTree ancestor-ring rendering (peopleTreeLayout already anticipates a family-node branch), the sign/hold/anchor wiring, then CUT 3 handshake-merge + CUT 4 memory reconciliation. Polish + kin-education pass (2026-06-15, operator: "frictionless, polished, enjoyable... educational info about kin — 3rd cousins, great uncle, cover every angle"): kinEducation.ts (pure + tested) turns any derived relationship label into a plain-English definition of the kinship term (parent/grandparent/great^N, aunt-uncle & grand-aunt lines, niece/nephew, cousin Nth Mx-removed with the share-an-ancestor math, in-laws & by-marriage) — sovereignty literacy pointed at family. The editor now renders each person as a card with a colored identicon avatar (KinAvatar, hues from key/node-id + initials), the relationship as a chip, the kin explanation inline, and animate-fresh-rise on the rows; the relation picker became Parent/Sibling/Spouse/Child chips with press animation; the header shows a "{N} people · {G} generations" count; and the person detail leads with a big avatar + the relationship chip + a "📖" teaching line explaining how you\'re related. Everything stays on the existing graph + storiesAbout substrate. Add-from-anyone polish (2026-06-15): the add logic was generalized from add-relative-of-ME to addRelative(resolveTarget) so you can grow the tree from ANY person — tap Mom and add her parents (your grandparents), her siblings (your aunts/uncles), and onward, which is how great-uncles and Nth cousins actually come into existence (previously only self-adjacent adds were possible, so a grandparent literally could not be added). One shared renderAddForm renders on both the main add-to-me view and a person detail grow-the-tree-from-name section; form state is shared (one view at a time) and cleared on view switch by an effect; sibling adds validate against the TARGET parent, not just yours. Speed-run explorer slice (2026-06-16, operator: "almost feel like you could speed run up and down the thing... the more you scroll up you go up the tree and then when you scroll down it gets wider and bigger you can go down different legs"): FamilyTreeExplorer.tsx is a focus-and-walk navigator that re-roots on ONE person and shows only their immediate neighbors -- parents in a tight CENTERED band above (the line CONVERGES as you climb toward the apex), the focused person big in the center with their spouse beside them, and their children fanned WIDER as a horizontally-scrollable row below (each child is a different leg DOWN). Tapping a parent ascends, tapping a child descends that leg, tapping the center person opens the existing PersonDetailView via the same onSelect(node, relationship) the canvas uses (detail is reused, never rebuilt); a breadcrumb names who you are viewing and a "Back to you" reset re-roots on selfId; explicit "to parents" / "to children" controls name the directions for discoverability while the relative cards stay the primary gesture. focusNeighbors.ts is the pure + tested walk engine (parents up / spouses across / children down, each named via relationshipLabel from the FOCUSED person and gendered, deterministic name-then-id ordering, edge-only and missing-node safe). The explorer takes the SAME Props as FamilyTreeCanvas so it is a drop-in alternate view; FamilyTreeEditor renders a "Tree | Explore" segmented toggle and code-splits the explorer behind React.lazy + Suspense so it ships as its own FamilyTreeExplorer-*.js chunk and never inflates HomeScreen\'s eagerly-imported static graph (Tree stays the default, so nothing existing changes by default). The explorer reads the real graph, not the canvas\'s ancestor-slot-augmented graph, since its own "no one recorded above" affordance covers missing parents. Friend-tree explorer reuse (2026-06-17, operator: "the same speed-run up/down explorer when viewing a friend\'s shared tree"): NOW BUILT. TreeCanvasArea was made the single reusable canvas-area for both contexts by making canvasGraph and onAddAncestor OPTIONAL -- the editor still passes both (dashed ancestor slots + the add-parent affordance on YOUR tree), and FriendTreesView now mounts TreeCanvasArea where it previously mounted FamilyTreeCanvas, passing only the friend\'s graph + open.rootNodeId as selfId + its existing read-only onSelect, omitting canvasGraph (canvas falls back to graph) and onAddAncestor (no add-ancestor affordance). The friend view gets the same "Tree | Explore" toggle and the same lazy FamilyTreeExplorer-*.js chunk for free; Tree stays the default so the friend view is visually unchanged until you tap Explore. Because selfId is the FRIEND\'s own node, every relationship label reads relative to the friend (correct for someone else\'s tree), and the explorer is navigation-only -- no editing leaks into the read-only friend context, and tapping the center person opens the same ReadOnlyPersonDetail FriendTreesView already showed.',
};
