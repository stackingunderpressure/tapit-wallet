import type { FeatureManifest } from '../../shared/lib/manifest.ts';

export const manifest: FeatureManifest = {
  slug: 'sign-request',
  born: '2026-05-21',
  purpose:
    "Layer 2 — the inter-app signing pathway via deeplink. A third-party app or another wallet constructs a URL pointing at /sign with a base64-encoded SignRequest in the query string. The wallet decodes it, renders a plain-English approval screen (per DESIGN.md §9 — the screen IS the product) showing the requesting origin, the host of the callback URL the operator's about to redirect to, and the concrete content being signed. On approve, the wallet builds the attestation via the right tapit-attest builder, signs with the active key, holds it, queues anchoring, and redirects to the callback URL with a SignGrant query param. On decline, redirects with a structured SignDecline. The wallet is the one place keys live; this is how every other app gets a signature without ever holding a key.",
  touches: [
    'src/features/sign-request/types.ts',
    'src/features/sign-request/parseSignRequest.ts',
    'src/features/sign-request/parseSignRequest.test.ts',
    'src/features/sign-request/renderRequest.tsx',
    'src/features/sign-request/approveRequest.ts',
    'src/features/sign-request/coSignEnvelope.ts',
    'src/features/sign-request/coSignEnvelope.test.ts',
    'src/features/sign-request/declineRequest.ts',
    'src/features/sign-request/SignApprovalScreen.tsx',
    'src/features/sign-request/signIn.test.ts',
    'src/features/sign-request/vaultTrail.ts',
    'src/features/sign-request/vaultTrail.test.ts',
    'src/features/sign-request/signPsbtCosign.ts',
    'src/features/sign-request/signPsbtCosign.test.ts',
    'src/features/sign-request/psbtCosignChannel.ts',
    'src/features/sign-request/usePsbtCosignRequests.ts',
    'src/features/sign-request/IncomingPsbtCosignBanner.tsx',
    'src/features/sign-request/vaultMembershipChannel.ts',
    'src/features/sign-request/useVaultMembershipRequests.ts',
    'src/features/sign-request/acceptVaultMembership.ts',
    'src/features/sign-request/acceptVaultMembership.test.ts',
    'src/features/sign-request/IncomingVaultMembershipBanner.tsx',
    'src/features/sign-request/psbtCosignResponseChannel.ts',
    'src/features/sign-request/approveRequest.test.ts',
    'src/features/sign-request/declineRequest.test.ts',
    'tapit-attest/src/core/sign-in.ts',
  ],
  depends_on: ['wallet-core', 'storage', 'anchoring', 'cosigning', 'circle-phrase'],
  pause_safe: false,
  removal_safe: false,
  monetizable: false,
  notes:
    "SignRequest is now a discriminated union on `intent`. 'attest' (original) — wallet creates a NEW signed envelope from kind/tier/subject/fields. 'cosign-existing' (added 2026-06-12, the Trailhead mutual-proof-of-presence atom) — the requester hands over an already-signed envelope and the wallet ADDS its signature, returning the merged multi-signature envelope; the claim is untouched so the canonical envelopeId is identical before and after. parseSignRequest validates the incoming envelope with parseEnvelope + verifyEnvelope and declines `invalid_envelope` if it doesn't parse or carries no valid signature (never co-sign garbage). The sign+merge logic is the pure coSignEnvelope() helper (wallet.sign then mergeSignatures dedupe+verify) so it's unit-tested directly; approveRequest branches on intent and reuses it. renderRequest shows a co-sign view (what it is + how many already signed) for the cosign branch; the approval button reads 'co-sign this'. depends_on gained cosigning (mergeSignatures). Still deeplink-only; Nostr NIP-46 transport stays OUT of v1 (a different transport sharing the same message shapes). SSO needs NO new intent — an app gets a wallet-signed session assertion today by requesting a `meta` attestation (nonce + issued_at + expires_at) via 'attest' and verifying the returned envelope against the user's pubkey. The shapes live in this feature for v1; hoist into tapit-attest when a fleet needs them shared. Remaining deferred intent: disclosure-proof. 'sign-in' (added 2026-06-22, sign-in by attestation cross-repo with DynastyTrust as verifier) — the requester (a relying party) mints a single-use SignInChallenge, persists it, and embeds it in the request; the wallet answers by signing the challenge and returns a SignInAttestation in the grant's `signIn` field (NOT `envelope` — a SignInAttestation is not an Attestation envelope; SignGrant.envelope is now optional and absent for sign-in grants). NOTHING is created/held/anchored and NO funds move — it is a one-time login proof carrying only the public key, the echoed challenge, and a Schnorr signature. The private key never leaves the Wallet: approveRequest computes the digest via tapit-attest's signInDigestFor(base) and signs through wallet.signDigest(digest); it deliberately does NOT call answerSignInChallenge() because that takes a raw private-key hex and would force extracting the key. signInDigestFor was added to tapit-attest/src/core/sign-in.ts as a thin pass-through to the existing internal signInDigest — the digest and base shape are unchanged so cross-repo verify parity (DynastyTrust's golden fixture) holds; signIn.test.ts proves the wallet path and answerSignInChallenge both verify against the same challenge, which is only possible if the exposed helper bytes equal the internal digest bytes. parseSignRequest validates the challenge shape (v===1, 32-byte hex nonce, non-empty audience, string timestamps) and declines invalid_request on a malformed challenge before the user is asked. 'psbt-cosign' (added 2026-08-06, Cut B stage B1 -- DynastyTrust docs/integration-phase1-signin-and-bridge.md and docs/build-map-and-cut-lists.md DT-4) -- a DynastyTrust vault-spend PSBT gets a signature from a key that lives in Tapit. This is NOT an attestation: no envelope, no hold, no anchoring queue entry -- it's a real BIP341 tapscript Schnorr signature over a real Bitcoin spend, built on the Cut B stage B0 shared module @dynastytrust/bip341-psbt-signer (vendored byte-identically from DynastyTrust; see that package's own header comment for the parity-test discipline). Three new files carry the weight: vaultTrail.ts defines and reads the vault-membership attestation shape (kind=agreement, agreement_type=vault-membership, fields naming the vault descriptor, role, the exact known tapscript leaf bytes this signer's key appears in, and an optional high-value threshold) and exposes findVaultTrail (a verified, wallet-signed membership for this exact vault, or null) plus isKnownLeafScript and requiresCallbackConfirmation (fail-closed: no declared threshold means the callback ritual is ALWAYS required, per docs/2026-08-callback-verification-and-amount-tiers.md). signPsbtCosign.ts is the pure signing core (mirrors coSignEnvelope.ts's own \"pure, no redirect, so it's unit-testable\" pattern): refuses with a typed PsbtCosignError when no trail exists (no_vault_trail), when a high-value spend hasn't had its callback confirmed (callback_required), or when no input's tapscript leaf byte-matches a script this wallet was told about at vault-creation time (unknown_leaf_script) -- the vault_descriptor label in the live request is NEVER sufficient on its own, matching the risk register's \"no rogue signing\" rule verbatim. Only once all three checks clear does it compute the BIP341 sighash (tapLeafHash + tapscriptSighash from the shared module) and sign via wallet.signDigest -- the same no-key-leak seam every other intent uses. approveRequest.ts's psbt-cosign branch is now a thin wrapper: call signPsbtCosign, wrap the resulting PSBT hex in the grant's new `psbt_hex` field (present ONLY for this intent), redirect. renderRequest.tsx's banner deliberately never trusts vault_context for the security-relevant facts: it re-parses psbt_hex itself and shows the REAL total output amount, every output's amount + scriptPubkey (hex, since neither repo has a P2TR bech32m encoder yet -- an honest, named gap, not a fabricated address), and whether the transaction carries a timelock (locktime > 0) as a plain-language hint that a recovery/inheritance path may be in play; vault_context.vault_descriptor is shown but explicitly labeled as the requester's own claim, not independently verifiable from the transaction bytes alone. SignApprovalScreen.tsx computes the SAME gates (findVaultTrail + requiresCallbackConfirmation) at render time from the live `holdings` so it can render a hard no-Approve-button block when there's no trail, or a callback-confirmation checkbox that must be ticked before Approve enables when the spend requires it -- but approveSignRequest/signPsbtCosign re-check both independently, so a UI bug can never sign an ungated spend (risk register: \"the human tap is the last gate, not the only gate; the wallet does its own verification first\"). Issuance -- DynastyTrust actually minting a vault-membership attestation at vault creation and delivering it to each member's Tapit wallet -- is explicitly NOT built here; this cut is the READ side + the signing gate only, so until issuance ships (a DynastyTrust-side follow-on, likely alongside B2), no wallet holds a matching trail for any real vault and psbt-cosign is correctly, honestly refused for everything -- fail-closed is the right default, not a bug to route around. Next: B2 (DynastyTrust's \"Sign via Tapit\" requester branch + mergePsbts absorb) and B3 (move the transport from deeplink to the encrypted Nostr inbox for multi-member vaults). B2 shipped 2026-08-06 (see manifest notes elsewhere for the deeplink new-tab flow). B3 slice 1 (2026-08-08, operator: \"it automatically returns the signature back to... dynasty trust automatically... over Nostr\") -- \"prove the pipe\" only, no signing yet: psbtCosignChannel.ts mirrors encryptedInbox.ts's sendEnvelopeTo/subscribeInbox shape exactly but for PsbtCosignSignRequest content on its own event kind (9576, the next free sibling after the liveness channel's 9575 -- a psbt-cosign request is not an Attestation, same reasoning that gave liveness its own kind). usePsbtCosignRequests.ts reads wallet/transport straight from WalletContext rather than being wired into WalletProvider.tsx's own state, since that file sits at its 800-line hard limit already; IncomingPsbtCosignBanner.tsx is the two-line mount in HomeScreen.tsx that makes receipt visible. Deliberately receive-only: no decrypt-and-act, no approveRequest call, no response published back yet -- that's slice 2, which will also need approveRequest.ts's psbt-cosign branch to grow a non-redirect delivery path (it currently hardcodes window.location.href to the callback URL; a Nostr-delivered request has no page to redirect, the response has to publish back over the same channel instead). Phone-callback phrase gate (2026-08-08, operator: 'the pin code is before hand so that you know for sure that is the person... push the red button... no one would sign') -- SignApprovalScreen.tsx's plain 'I verified this by phone' checkbox is now backed by a real phrase check where one is on file. The screen calls hasCirclePhrasePair (new circle-phrase feature) to decide: if the vault has no phrase pair delivered yet, the ORIGINAL checkbox still renders unchanged -- no dead end, no vault ever becomes unsignable because a phrase pair was never set up. If a pair IS on file, the checkbox is replaced by a phrase-entry field; checkCirclePhrase hashes the entry (PBKDF2, never stores the plaintext) and compares against both stored hashes. A match on the NORMAL phrase sets calloutConfirmed=true exactly as the checkbox used to -- signPsbtCosign's own re-check is completely unchanged, so the actual signing gate is identical to before this cut. A match on the DURESS phrase leaves calloutConfirmed false (Approve stays disabled, same as any unconfirmed state) and swaps the amber callback card for a serious red 'do not approve' block instructing the operator to contact their circle or the authorities right now on a different channel, and to use the vault's halt control if it has one (DynastyTrust VaultDetail, a separate change in that repo) -- signing that specific request is blocked entirely by the existing calloutConfirmed=false path; this cut deliberately does NOT attempt an automatic cross-app duress signal, because a psbt-cosign request's sender is an EPHEMERAL keypair by design (tapit-nostr-cosign.ts's own header comment) and DynastyTrust's write-side APIs are JWT-authenticated -- Tapit has no session to call them with, and there is no reliable way from inside this request alone to identify WHICH circle member's key should be the duress flag's subject. Rather than guess, the response here stays a clear, unambiguous instruction for the human to close the loop by voice, exactly as originally described, while the deeper cross-app duress-signal wire (probably riding the same Nostr channel B3 slice 2 will use for the signed response) is left as a named follow-on, not silently faked. Cut C3 (2026-08-08, operator: 'my circle... they are my watch towers... they only help me move it when they know I seriously want to move it') -- vaultTrail.ts's own header named issuance as explicitly not built; this cut builds it. DynastyTrust cannot hand this wallet a pre-signed vault-membership attestation (it has no Tapit identity or private key), so it sends a REQUEST describing the claim fields instead -- vault_descriptor, vault_name, role, the compiled tapscript leaf bytes that role's key appears in, and an optional high-value threshold -- over its own Nostr kind (9578, next free sibling after the circle-phrase channel's 9577; vaultMembershipChannel.ts, mirroring psbtCosignChannel.ts's verify-then-decrypt-then-shape-check discipline exactly). useVaultMembershipRequests.ts holds incoming requests in local state (same WalletContext-reading pattern as usePsbtCosignRequests.ts) but, unlike that receive-only hook, also exposes dismiss(eventId) because this cut ships a real accept/decline UI, not just pipe-proving. acceptVaultMembership.ts is the pure, unit-tested mint step: wallet.attest({kind:'agreement', tier:'high_stakes', subject:vault_descriptor, fields:{agreement_type:'vault-membership', ...}}) self-signs with the wallet's OWN key -- the one fact that actually satisfies findVaultTrail's signedByMe check, which a bare envelope hold of someone else's signature never could -- then holds it and queues anchoring exactly like approveRequest.ts's generic 'attest' branch does, but never touches window/navigation since a Nostr-delivered request has no callback URL to redirect to. IncomingVaultMembershipBanner.tsx (mounted in HomeScreen.tsx above IncomingPsbtCosignBanner) is the visible half: names the vault and role, explicitly says accepting signs nothing and spends nothing, Accept mints+holds, Decline just dismisses (no signal sent back to DynastyTrust -- there is no decline channel, matching this cut's scope). DynastyTrust's side (circle-membership-delivery.ts, tapit-circle-members.ts, VaultMembershipSetup.tsx, and the compiler's new leaf_scripts field on /compile's response, verified byte-identical to what psbt-binary actually attaches via a new Rust test) lives in that repo. Deliberately scoped to role='founder' only for now, matching the operator's stated model (circle members ARE founders/watchtowers, not a separate heir/protector cohort) -- heir and protector roles are typed and handled correctly end-to-end if DynastyTrust ever sends one, just nothing in that repo constructs one yet. What Cut C3 does NOT do: no decline notification back to the requester, no re-request/expiry handling for a request that arrives before the recipient's wallet is online (Nostr's own relay replay/backfill is the only retry mechanism), and accepting is a one-way trust decision with no later 'revoke my membership' affordance -- all named follow-ons, not silent gaps. Cut #22 (2026-08-08, same session, operator: 'only thing my helper sees is hey tom wants to spend call him check on him get the rolling code and bam my transaction is signed in the background') -- collapses the psbt-cosign approver experience down to exactly that. renderRequest.tsx's psbt-cosign branch no longer parses psbt_hex to show total sats, per-output amounts, destination scripts, or timelock detail -- it now says only who's asking and which vault, plus the same 'call them, get the code' instruction the amber card always had. This is a deliberate trust shift, not an oversight: the circle member's job is verifying the PERSON on a live call, not auditing a transaction they have no context to judge anyway; showing a dollar figure only gives a coerced or fooled approver something to rationalize against. Nothing about the actual security boundary moved -- signPsbtCosign.ts still independently matches the request's tapscript leaf against the held vault-membership trail before it will sign, exactly as before, regardless of what the screen displays. SignApprovalScreen.tsx's approve() is now a thin wrapper over a new doApprove(confirmed: boolean), which takes the confirmation as a parameter instead of reading calloutConfirmed off state -- necessary because verifyPhrase needed to trigger it the instant a phrase comes back 'normal', before a state update could re-render. That's the 'bam': typing the correct phrase and hitting 'Confirm & sign' (renamed from 'Check') now signs immediately, with no separate manual Approve tap in between. The generic Approve button at the bottom of the screen is hidden specifically when a callback-gated psbt-cosign request has a phrase pair on file, since the phrase field IS the approve action in that case; every other path (sign-in, cosign-existing, a plain attest, or the no-phrase-pair checkbox fallback for a vault that never got a phrase pair set up) keeps the original explicit two-step button unchanged, so nothing that used to work stops working. The no-trail hard-stop and the duress red-block both still render exactly as before -- those are real gates, not decoration, and neither one is amount/destination information. Cut B3 slice 2 (2026-08-08, same session, closes the loop slice 1 left open) -- a psbt-cosign request that arrived over Nostr now gets a real response instead of vanishing into a redirect nobody's listening for. `PsbtCosignSignRequest` gained an optional `response_channel: {kind:'nostr', requester_pubkey}`, set by DynastyTrust's tapit-nostr-cosign.ts to this request's own ephemeral reply pubkey and parsed through by parseSignRequest.ts. approveSignRequest now takes a `transport: Transport | null` parameter and returns `ApproveResult` (`{delivered:'redirect'}` for the unchanged B2 deeplink path, `{delivered:'nostr'}` for the new one); when a psbt-cosign request carries response_channel, it calls the new psbtCosignResponseChannel.ts's sendPsbtCosignResponseOverNostr instead of setting window.location.href -- published under the WALLET'S OWN real identity (not an ephemeral sender), the same choice encryptedInbox.ts's sendEnvelopeTo makes for a wallet-originated send, on its own kind (9579, next free sibling after the vault-membership channel's 9578). declineSignRequest got the same treatment: a psbt-cosign decline with a response_channel returns `{delivered:'none'}` instead of redirecting into a callback URL DynastyTrust never expects a browser to land on (there's no decline channel over Nostr for this cut -- a missing signature already tells the requester nothing signed). SignApprovalScreen.tsx pulls `transport` from WalletContext, passes it through doApprove, and calls `navigate('/')` (react-router, in-app) whenever a result comes back `'nostr'`/`'none'`, since nothing else will move the screen off 'Signing…' once window.location was never touched. IncomingPsbtCosignBanner.tsx stopped being receive-only: each incoming request gets a Review button that navigates to `/sign?req=<b64url>` with response_channel set to the pubkey the request actually arrived from, landing on the SAME SignApprovalScreen a deeplink request uses -- every gate built for B1/Cut C3/the phrase-auto-sign cut (the attested-trail check, the callback-required phrase gate, the duress block, the auto-sign-on-correct-phrase flow) applies identically, with zero duplicated logic. approveRequest.test.ts and declineRequest.test.ts are new -- approveRequest.ts had never been unit-tested directly before (only its sub-pieces signPsbtCosign/coSignEnvelope were), but this specific branch is new control flow worth locking in on its own; the Nostr-response test decrypts the published event with a stand-in requester keypair and asserts the payload actually carries the signed PSBT, not just that publish was called. DynastyTrust's side (tapit-nostr-cosign.ts returning the ephemeral reply keypair, the new psbt-cosign-response-channel.ts subscriber, and NotifyCircleViaNostr in VaultDetail.tsx keeping a long-lived NostrTransport + per-signer subscription open for the life of a signing session, merging a response straight into `signing` via the existing externalImport path) lives in that repo. addressedToMe diagnostic (2026-08-10, operator: \"it has to be that Tapit is not doing something right once it receives it\") -- after the wire-level counter proved 128 events were genuinely reaching this device's subscriptions and every one still failed MAC verification on decrypt, the open question was whether these events were even addressed to this wallet in the first place; a busy shared public relay is not app-scoped infrastructure and relay-side '#p' filtering is not something a client can verify from the outside. nostrEvent.ts gained eventPTags(event), and both psbtCosignChannel.ts's and vaultMembershipChannel.ts's decrypt_failed branch now checks whether any of the event's own 'p' tags match recipient.keyHistory, recording the answer as addressedToMe=true/false in the diagnostic detail -- true means the event really was encrypted to a key this wallet holds and the failure is a genuine crypto/data mismatch; false means the event was never addressed to this wallet at all (someone else's traffic on the same shared relay), which is not a Tapit decrypt bug. vaultMembershipChannel.ts had no channelDiagnostics instrumentation before this cut (only psbtCosignChannel.ts did); it now gets the identical verify_failed/decrypt_failed/parse_failed/schema_failed/delivered treatment, plus its own describeSchemaFailure. NostrActivitySection.tsx's diagnostics list now labels each row by channel (spend request / vault invite) since both channels share one diagnostics ring buffer.",
};
