import type { FeatureManifest } from '../../shared/lib/manifest.ts';

export const manifest: FeatureManifest = {
  slug: 'liveness',
  born: '2026-06-22',
  purpose:
    "The green / no-report / red surface — a quiet way for a person to tell the people they trust that they are OK, and to see at a glance whether those people are. Slice 1: a local store + a plain-English panel over the tapit-attest liveness primitive. The wallet mints a self-signed proof-of-life heartbeat (green while fresh, no-report once stale) and can raise a duress red flag on itself or a chosen circle member; red dominates everything and only counts from chosen people. The private key never leaves the Wallet — both actions sign through wallet.signDigest via the proofOfLifeDigestFor / duressFlagDigestFor helpers, the same no-key-leak seam the sign-request sign-in path uses.",
  touches: [
    'src/features/liveness/liveness.ts',
    'src/features/liveness/liveness.test.ts',
    'src/features/liveness/LivenessPanel.tsx',
    'src/features/liveness/manifest.ts',
    'src/features/transport/livenessChannel.ts',
    'src/features/transport/livenessChannel.test.ts',
    'src/features/recovery/KeychainTab.tsx',
    'src/features/wallet-core/WalletContext.ts',
    'src/features/wallet-core/WalletProvider.tsx',
    'src/features/connections/findVouchingCircleCandidates.ts',
    'scripts/bundle-budget.mjs',
    'tapit-attest/src/core/liveness.ts',
  ],
  depends_on: ['wallet-core', 'transport', 'connections'],
  pause_safe: true,
  removal_safe: true,
  monetizable: false,
  notes:
    "Slice 1 of the 'green ladder'. The liveness signals (ProofOfLife, DuressFlag) are NOT Attestation envelopes, so they do not ride the encrypted inbox (sendEnvelopeTo / subscribeInbox), whose receive half runs parseEnvelope -> assertWellFormed and would reject them. TRANSPORT PATH B (DONE): network propagation now rides the dedicated encrypted liveness channel src/features/transport/livenessChannel.ts on its OWN wire kind TAPIT_LIVENESS_KIND = 9575 (a sibling to TAPIT_ENVELOPE_KIND 9573, distinct from the retired chat kind 9574 and NIP-17 kind 1059), exactly as NIP-17 chat rides its own kind rather than the envelope kind. sendLivenessSignal serializes the LivenessSignal wrapper, NIP-44-encrypts it to the recipient via wallet.nip44EncryptTo, buildEvents it on the liveness kind with a p tag, and publishes; relays see only ciphertext. subscribeLiveness verifies the outer event, decrypts via nip44DecryptFromAnyKey, JSON-parses, then re-verifies the INNER signal with verifyProofOfLife / verifyDuressFlag (NEVER parseEnvelope) so a forged heartbeat or red flag from a relay or stranger never reaches the handler, dedups by event id, and hands the signal to applyIncomingSignal. The seam is wired by two thin optional adapters in liveness.ts — createTransportSendSignal (pass as createLivenessStore's sendSignal) and subscribeLivenessStore (folds inner-verified arrivals into the store). The store itself stays transport-agnostic and no-op-safe: createLivenessStore with no sendSignal still works fully for the local-only surface, so depends_on adds transport without making the local store require it. That keeps the one-envelope standard untouched: liveness gets its own wire kind and never pretends to be an Attestation. Clearing a raised red flag is deliberately omitted from slice 1 — it is a higher-layer quorum concern (the primitive holds reds until a human clears them; an alarm that times itself out is worse than one that persists). FIELD-TEST MOUNT 2026-06-23 ('make the liveness feature field-testable'): LivenessPanel is now mounted as a collapsible 'Liveness / circle check-in' section in the Keychain tab (src/features/recovery/KeychainTab.tsx, lazy-loaded, matching the Social-recovery sibling-section pattern), and wired to the LIVE Mycelium transport the encrypted inbox + chat already ride. WalletProvider now exposes its existing `transport` state through WalletContext.transport (the SAME instance — no second connection); LivenessPanel reads it via useWallet(), backs the store's sendSignal with createTransportSendSignal(transport, wallet) through a transportRef so the send path follows network availability without rebuilding the store, and runs subscribeLivenessStore(transport, store, wallet) in a useEffect keyed on (transport, store, wallet) with sub.close() on unmount — the exact lock/unlock/opt-out/rotation lifecycle the inbox + useChatTransport subscriptions use. A null transport is a no-op everywhere (send returns early, the receive effect bails), so the panel stays fully usable on-device and the feature remains pause_safe / removal_safe: deleting the Keychain section + the WalletContext.transport field leaves a working app. Added a gentle proof-of-life nudge — a calm amber prompt ('It's been a while … your last check-in was N ago … no rush') shown only after at least one check-in AND past the 24h ttl, never alarming — plus a 'Last check-in: N ago' line, both driven by a 1-minute interval tick. Honest network copy: the header now states whether check-ins travel to the circle (encrypted) or stay on-device when the network is off, replacing the old 'device only' note. No key is touched — raising red is still the two-step confirm, signatures still mint through wallet.signDigest inside the store, relays still see only ciphertext via the channel. pause_safe + removal_safe hold. CIRCLE-FROM-CONNECTIONS 2026-06-25 ('your circle should have options to be filled by people you already handshaked with'): the circle section now surfaces a 'From your connections' one-tap add list above the paste-a-key field, sourced from findVouchingCircleCandidates(holdings, wallet.identity) — the SAME tested substrate the vouching-circle + recovery + share-tree pickers use (family-unit, recovery-cohort, and handshake peers, deduped by pubkey, name resolved family > cohort > handshake). Matched on the stable genesis identity, not the active publicKey, so a key rotation never drops a known peer from the list. Candidates already in the circle are filtered out; tapping one routes through the shared addToCircle() validator the paste field uses (64-hex shape check + de-dupe), so both paths enforce identical rules. The paste field stays as the fallback for someone not yet handshaked. Depends_on now includes connections; the dependency is the pure candidate-finder helper, the same deep-import path GatedLeafSection/friends-trees/recovery already consume. pause_safe + removal_safe still hold — the list is additive UI and degrades to just the paste field when the operator has no connections yet.",
};
