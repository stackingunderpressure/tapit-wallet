import type { FeatureManifest } from '../../shared/lib/manifest.ts';

export const manifest: FeatureManifest = {
  slug: 'disclosure',
  born: '2026-05-21',
  purpose:
    'Selective leaf disclosure (DESIGN.md §8). The "prove I am over 21 without revealing my birthday" demonstration. From a held attestation, the operator picks one or more leaves of the claim and the wallet produces a tapit-attest MultiDisclosureProofBundle — a pruned Merkle tree carrying just the disclosed leaves with non-disclosed siblings replaced by their hashes. The operator hands it to a verifier; the verifier opens the wallet PWA at /verify (no auth required — they may not have a wallet), pastes the proof, and the wallet runs verifyMultiDisclosureProof against the recomputed claim root + canonical envelope digest. Single-leaf and multi-leaf go through the same primitive. The verifier route also still accepts legacy single-leaf DisclosureProofBundle (parseDisclosureProof discriminates by structure) so proofs minted by older wallets still verify.',
  touches: [
    'src/features/disclosure/ShareProofModal.tsx',
    'src/features/disclosure/VerifyProofScreen.tsx',
    'src/features/disclosure/HowVerificationWorks.tsx',
    'src/features/disclosure/verifyProofAnchor.ts',
    'src/features/disclosure/verifyProofAnchor.test.ts',
    'src/features/disclosure/parseDisclosureProof.ts',
    'src/features/disclosure/leafIndex.ts',
    'src/features/disclosure/ShareCard.tsx',
    'src/features/disclosure/QuickShareModal.tsx',
    'src/features/disclosure/buildVerifyUrl.ts',
    'src/features/disclosure/buildVerifyUrl.test.ts',
    'src/features/disclosure/quickSharePresets.ts',
    'src/features/disclosure/exportProof.ts',
    'src/features/disclosure/exportProof.test.ts',
  ],
  depends_on: ['wallet-core', 'connections'],
  pause_safe: false,
  removal_safe: false,
  monetizable: false,
  notes:
    "The /verify route lives outside AuthGate because the verifier is a third-party context — anyone with a copy of the PWA. The verifier never sees the operator's wallet, only the proof bundle they pasted, and the math is the truth. ShareProofModal uses checkboxes (multi-select) and calls multiDisclosureProof for both one-field and many-field cases; the bundle shape is one pruned tree, not N stacked single-leaf bundles, so the wire payload stays small as the selection grows. Cut 7 (2026-05-24) added the Fresh share-card surface: ShareCard renders a 9:16 screenshot-targeted card carrying the assertion, the Bitcoin block stamp, and a verifier URL; QuickShareModal mints the proof and base64url-encodes it into a /verify?p=<bundle> URL when the encoded size fits under 1.8KB so the verifier taps once and the math runs automatically. 2026-06-05: that proof-mint + verify-URL logic was extracted into buildVerifyUrl.ts (returns {json, verifyUrl, urlIsInline}) so QuickShareModal and the journal stamped-photo corner QR build the identical one-tap link and can never drift; the journal feature consumes it to disclose the attachment_sha256 leaf into the QR burned onto a shared photo copy. The brief listed four Quick-share presets but only two are buildable today (verified-profile + organization-membership); the over-18 / over-21 presets need a birthday leaf added to the founding identity attestation, which is a structural identity-model change deferred to a future cut. Portable-verify cut 2026-06-11 (operator: make the proof verifiable by anyone OUTSIDE the system with math, not only inside our /verify page — the actual added value of an attestation layer): exportProof.ts holds the one pure helper (otsBytesFromHex — decode an anchor's hex OTS blob to raw .ots file bytes, throwing loudly on malformed hex so we never hand out a broken proof file). VerifyProofScreen now carries a 'Verify this without this page' block on every result: a Copy-proof button (the proof JSON is self-contained signature + Merkle math that re-checks in any compatible verifier with no server), and — once the anchor has already parsed+verified here (anchorCheck confirmed|pending) — a 'Download Bitcoin timestamp (.ots)' button that emits a STANDARD OpenTimestamps detached proof. This is genuinely standard because tapit-attest core/ots-codec serializeOtsProof/parseOtsProof read+write the real OTS magic, and the anchoring verifier confirms parsed.fileDigest === the envelope digest, so the downloaded .ots verifies with the canonical `ots` client or opentimestamps.org against the digest the page shows (also surfaced now), against the public Bitcoin chain — no app, no server, no trust in this page. Share-side completion (same day): the DOM download was consolidated into exportProof.downloadOtsFile (no-throw wrapper over otsBytesFromHex) and reused by the verify screen AND both share surfaces — QuickShareModal (Fresh) and ShareProofModal (Classic) now offer 'Download Bitcoin timestamp (.ots)' whenever the shared proof carries an anchor (ShareProofModal threads anchor.proof through its generated step as otsProofHex; QuickShareModal reads preset.attestation.anchor), so the owner hands out an independently-verifiable .ots from the moment the proof is minted, not only when a recipient lands on /verify. The proof JSON was already copyable from both share surfaces. Each share-side .ots button carries a plain-language caption (sovereignty-literacy: it tells the owner the file lets the recipient confirm the Bitcoin timestamp themselves with standard OpenTimestamps tools, no app and no trusting us). Share-QR preloads the verifier (2026-06-16): ShareProofModal QR previously encoded the raw proof JSON, so a plain camera scan opened nothing. It now encodes the /verify?p= link (via buildVerifyUrl) when the proof fits the inline byte budget, so a normal camera scan opens the verifier with the proof already loaded and checked; it falls back to the JSON QR + copy/paste when the proof is too large. The verify screen already auto-decodes ?p= and supports paste/Scan-QR, so this just hands the recipient the easy path.",
};
