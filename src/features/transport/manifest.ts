import type { FeatureManifest } from '../../shared/lib/manifest.ts';

export const manifest: FeatureManifest = {
  slug: 'transport',
  born: '2026-05-22',
  purpose:
    "Phase 5c-i-β of the Mycelium peer network — the wire layer that lets two wallets exchange tapit-attest envelopes across distance through encrypted Nostr events (D-06, D-11). A Transport-agnostic interface (publish, subscribe, close) so the network substrate is swappable; a minimal Nostr WebSocket client behind that interface; an encrypted inbox that wraps each envelope in NIP-44 v2 ciphertext (recipient-addressed in a `p` tag) and unwraps incoming events back into typed Attestation objects. Relays only ever see ciphertext — the social graph is private (MYCELIUM_NETWORK_SPEC §9). Default relay set ships with the wallet and is replaceable by the user (D-11a). Tapit envelopes ride inside a custom event kind (TAPIT_ENVELOPE_KIND = 9573); NIP-46 stays reserved for the separate app-to-wallet sign pathway (D-11c). Chat messages ride NIP-17 gift-wrapped events (kind 1059, see src/features/transport/nip17.ts) — three-layer nested wrap with an ephemeral key on the outer signature so the relay never sees the real sender, and a Nostr-standard kind so every modern relay stores it for offline retrieval (replaces the original custom kind 9574 which public relays did not reliably persist). The sendChatMessageTo + subscribeChatMessages helpers in encryptedInbox.ts are the public API; the nip17.ts module owns the gift-wrap construction and unwrapping primitives. Tier 1 of the three-tier message taxonomy in the per-peer chat roadmap brief — signed and encrypted but not an attestation and not anchored. Promotion to a full envelope happens at the UI layer.",
  touches: [
    'src/features/transport/transport.ts',
    'src/features/transport/nostrEvent.ts',
    'src/features/transport/nostrTransport.ts',
    'src/features/transport/nip17.ts',
    'src/features/transport/encryptedInbox.ts',
    'src/features/transport/nostrProfile.ts',
    'src/features/transport/nostrProfile.test.ts',
    'src/features/transport/nostrNote.ts',
    'src/features/transport/nostrNote.test.ts',
    'src/features/transport/connectWallet.ts',
    'src/features/transport/defaultRelays.ts',
    'src/features/transport/InboxPanel.tsx',
    'src/features/transport/envelopeRoute.ts',
    'src/features/transport/envelopeRoute.test.ts',
    'src/features/transport/NostrIndicator.tsx',
  ],
  depends_on: ['cosigning', 'connections', 'wallet-core'],
  pause_safe: true,
  removal_safe: false,
  monetizable: false,
  notes:
    'The wallet key is reused as the Nostr identity (D-11d) — same x-only BIP340 pubkey, same Schnorr signature. The Wallet class exposes signDigest, nip44EncryptTo, and nip44DecryptFrom so this feature never sees the raw private key (D-03). encryptedInbox is the helper layer; connectWallet is the one-call entry point that opens a NostrTransport (or accepts an injected one for tests), subscribes the inbox, and returns a handle whose close() tears everything down. The Nostr WebSocket client auto-reconnects with exponential backoff and dedupes events by id across relays; persistent offline outbox + sync resume are deferred to 5c-iii. WalletProvider opens the transport on unlock when the operator has enabled the nostrTransportEnabled pref (default false — subscribing exposes the wallet pubkey to public relays), and InboxPanel renders incoming envelopes at the top of the People tab. 5c-i-ε added inbox routing: a one-signature handshake opens the CosignAsWitnessModal pre-loaded with the envelope; a two-signature handshake opens the AbsorbCosignModal pre-loaded. Memberships and other envelope kinds fall back to the Copy action until their auto-receive path lands. Phase 8 Phase E2 extends envelopeRoute with a `self-membership-receive` action that fires when an incoming envelope is a self-membership credential (isSelfMembership from createMembership.ts) — the joiner-side Phase E2 cut surfaces these as "Accept join request" rows so an org-host wallet routes them through the same dispatcher every other arrival uses. Phase 8 Phase E3 cut 1 (Option 3 hybrid substrate) replaced the placeholder acceptor behavior with structural join-policy gating: HomeScreen.acceptSelfMembership now passes the org wallet\'s own self-declaration + holdings into receiveSelfMembership, which runs the evaluateJoinPolicy helper from src/features/governance/ before holding the envelope. The routing surface itself did not change — InboxPanel still dispatches `self-membership-receive` via envelopeRoute exactly as it did in Phase E2; the gating happens downstream so the inbox-to-acceptor wire stays stable across phases. Cut 2 will layer the pending-roster buffer + roster-publication envelope on top without touching either layer. envelopeRoute.test.ts ships 8 dispatch tests asserting the routeFor verdict for self-membership, org-issued membership, single-signed Tier P handshake, counter-signed Tier P handshake, a single-signed Tier R remote handshake, plus three vouch-loop-back cases — the dispatcher knows about every shape and a future addition cannot quietly steal a route from an earlier one. Vouch loop-back routing (Phase E4 polish 2026-05-27): routeFor gained an optional receiverPubkey second parameter so the joiner-side arrival of a peer-cosigned self-membership routes to absorb-cosign instead of the org-side self-membership-receive path. The disambiguator runs only for self-memberships and only when the receiver IS the envelope subject (the joiner) AND the envelope already carries more than the joiner\'s own signature (so the cosig fan-out copies the joiner never sees in their own inbox are not misrouted on edge cases). InboxPanel and ScanEnvelopeModal both pass useWallet().identity?.subject; the org-side accept path is preserved when the receiver is anything other than the joiner. Phase E4 cut 5 (peer-side vouch loop close, 2026-05-27) added the vouch-witness InboxRouteAction and its routeFor branch: when the envelope is a 1-signature self-membership AND receiverPubkey is neither the envelope subject (joiner) NOR the org named in the org_id leaf, routeFor returns a vouch-witness action with label "Vouch" and the hint "A peer is asking you to vouch for their join request." HomeScreen.routeInbox dispatches this action to a new VouchWitnessModal in src/features/cosigning/ that frames the decision as personal trust and sends the signed vouch back to the joiner via the same encryptedInbox sendEnvelope path the cosigning modals use. The third structural gate (receiver IS the org named in org_id, falls through to self-membership-receive) locks the discriminator so direct-submission joins to the org are not stolen by the vouch-witness branch — envelopeRoute.test.ts now ships 10 dispatch tests asserting all three self-membership receiver positions stay correctly routed. depends_on lists cosigning (encryptedInbox uses parseEnvelope; InboxPanel routes to the cosign modals), connections (the isHandshake / isSelfMembership predicates), and wallet-core (InboxPanel reads inbox state from WalletContext); removal_safe is false because HomeScreen imports InboxPanel. NIP-17 chat migration (2026-05-26 evening) replaced the previous custom-kind 9574 chat path with the Nostr-standard kind 1059 gift-wrap shape. Root cause: many public Nostr relays in DEFAULT_RELAYS accept-but-do-not-persist events whose kind sits outside well-known ranges, so the operator-reported "the other person is not receiving the message either way" bug was a relay storage policy issue, not a wallet bug. The migration kept the sendChatMessageTo + subscribeChatMessages public surface unchanged so WalletProvider.sendChatMessage and the chat-subscription effect in WalletProvider need no callsite changes; the wire shape underneath changed from one-layer NIP-44 + custom kind to NIP-17 three-layer (rumor kind 14 → seal kind 13 signed by sender → gift wrap kind 1059 signed by per-message ephemeral key). Privacy upgrade: the relay no longer sees the real sender pubkey on a chat publish — only the ephemeral wrapper key — so traffic analysis against the social graph gets harder. The new src/features/transport/nip17.ts module exports NIP17_GIFT_WRAP_KIND / NIP17_SEAL_KIND / NIP17_CHAT_RUMOR_KIND constants plus buildGiftWrap + unwrapGiftWrap helpers; encryptedInbox.ts re-uses them via the same sendChatMessageTo / subscribeChatMessages public functions. transport.test.ts updated: chat tests assert kind 1059 + outer-pubkey-is-NOT-the-real-sender, and the two malformed-payload tests construct kind-1059 events with garbage inner ciphertext via the raw encryptTo + generateKeypair + signDigest primitives from tapit-attest. The TAPIT_CHAT_KIND constant was removed from nostrEvent.ts since no production code still references it; the old custom kind retires cleanly. Key-rotation receive fix (operator bug 2026-05-31, two cuts). Cut 1 widened the subscription #p filters: subscribeChatMessages + subscribeInbox now filter on recipient.keyHistory (genesis + every rotated key) not recipient.publicKey, so gift-wraps and envelopes a peer addressed to a pre-rotation key still match and arrive — encryptedInbox.test.ts asserts both filters cover historical keys. Cut 2 fixed the decrypt half in tapit-attest + the two consumers: Wallet.rotate() previously discarded the retiring private key, so even a delivered old-key message could not be opened (verified by probe: nip44DecryptFrom threw MAC-failure). The Wallet now retains retired keypairs (new optional WalletSnapshot.retiredKeypairs field, persisted through snapshot/fromSnapshot so the fix survives save+reload; hard-private #retiredKeypairs field) and exposes nip44DecryptFromAnyKey which tries the active key first then each retired key. encryptedInbox.handleIncoming and both nip17 unwrap layers (gift-wrap + seal) call nip44DecryptFromAnyKey, and unwrapGiftWrap recipient-named check now accepts any key in keyHistory. Security posture unchanged: retired private keys live only inside the wallet and ride the same always-encrypted snapshot as the active key (D-03). KNOWN LIMITATION: this protects rotations from this fix forward — a key already rotated away BEFORE the fix shipped was discarded and is unrecoverable except by restoring a pre-rotation backup. encryptedInbox.test.ts adds three decrypt-across-rotation tests (old-key unwrap, current-key still works, snapshot round-trip).',
};
