import type { FeatureManifest } from '../../shared/lib/manifest.ts';

export const manifest: FeatureManifest = {
  slug: 'circle-phrase',
  born: '2026-08-08',
  purpose:
    "The phone-callback phrase pair (docs/2026-08-callback-verification-and-amount-tiers.md's callback ritual, made concrete per the operator's 2026-08-08 request). A Tapit Circle vault owner picks ONE shared normal phrase and ONE shared duress phrase for their circle and sends both, once, NIP-44 encrypted, to every member's Tapit wallet. During a live phone call verifying a spend request, the caller reads back one of the two phrases; the listener types whatever they heard into this wallet before Approve unlocks. A normal-phrase match proceeds to sign; a duress-phrase match silently blocks signing and raises a real duress flag instead (sign-request feature). Nothing here signs anything itself -- this feature only stores the phrase pair (hashed, never plaintext) and answers 'which phrase was that.'",
  touches: [
    'src/features/circle-phrase/circlePhrase.ts',
    'src/features/circle-phrase/circlePhrase.test.ts',
    'src/features/circle-phrase/circlePhraseChannel.ts',
    'src/features/circle-phrase/useCirclePhraseDeliveries.ts',
    'src/features/circle-phrase/CirclePhraseReceiver.tsx',
    'src/features/circle-phrase/CirclePhraseSection.tsx',
    'src/features/circle-phrase/circlePhraseAckChannel.ts',
    'src/features/circle-phrase/remoteCirclePhraseStore.ts',
    'supabase/migrations/20260813000001_create_circle_phrase_backups.sql',
  ],
  depends_on: ['wallet-core', 'transport', 'storage'],
  pause_safe: true,
  removal_safe: true,
  monetizable: false,
  notes:
    "circlePhrase.ts is the storage + verification core, deliberately independent of transport: storeCirclePhrasePair(vaultDescriptor, vaultName, normalPhrase, duressPhrase) salts a fresh 16-byte random salt and PBKDF2-SHA256 hashes (210,000 rounds, matching the round count DynastyTrust documents for its own passphrase material) BOTH phrases before anything touches IndexedDB -- the plaintext is never written anywhere, only ever held in a local variable long enough to hash it. checkCirclePhrase(vaultDescriptor, entered) hashes the caller's entry with the stored salt and compares against both stored hashes, returning a typed result ('normal' | 'duress' | 'no-match' | 'not-configured' | 'locked') rather than throwing, so the approval screen can branch cleanly. Five wrong guesses locks that vault's phrase check for five minutes (a UX speed bump against a stolen-device brute force; the KDF cost is the real boundary, not the lockout). Storage is one aggregate record keyed 'circlePhrase:v1' in the shared idb wrapper (same IndexedDB the wallet blob itself uses), keyed internally per-vault by sha256(vaultDescriptor) so a listing read is a single get. circlePhraseChannel.ts is the receive-only Nostr half: a new event kind (9577, the next free sibling after the psbt-cosign channel's 9576) carries a NIP-44-encrypted {v, vault_descriptor, vault_name, normal_phrase, duress_phrase} payload, verified (event signature + id) then decrypted then shape-checked before ever reaching a handler -- same discipline as psbtCosignChannel.ts and the liveness channel. useCirclePhraseDeliveries.ts reads wallet/transport straight from WalletContext (WalletProvider.tsx is at its 800-line hard limit, same reasoning usePsbtCosignRequests.ts documents) and, unlike that receive-only hook, stores IMMEDIATELY on receipt rather than holding state for a later approve step -- there is no approval gate for accepting a phrase pair, only for later USING one. CirclePhraseReceiver.tsx is the ~2-line mount in HomeScreen.tsx (matching IncomingPsbtCosignBanner's footprint) that shows a transient 'safety phrase saved' confirmation; the durable status view (CirclePhraseSection.tsx, which vaults have a pair, when received, whether locked out) lives in Settings where there was still headroom under its own file-size limit. The consuming side -- SignApprovalScreen.tsx replacing its plain calloutConfirmed checkbox with a real phrase-entry gate, and raising a duress flag on a duress-phrase match -- lives in the sign-request feature, not here; this feature only answers 'which phrase was that,' it never decides what to do about it. Real-receipt ack (2026-08-11, operator: \"message couldn't drop in that situation\" -- a relay accepting the original delivery publish never proved this wallet actually got it): CirclePhraseDelivery gained an optional response_channel field (mirrors PsbtCosignSignRequest's and VaultMembershipRequestPayload's own field), backward compatible -- its absence is not a schema failure. New circlePhraseAckChannel.ts (kind 9581, next free sibling after the vault-membership ack channel's 9580) publishes under the wallet's own real identity, structurally identical to vaultMembershipAckChannel.ts / psbtCosignResponseChannel.ts. useCirclePhraseDeliveries.ts's storeCirclePhrasePair().then() now fires a best-effort ack immediately after the pair is actually written to IndexedDB -- not before, and not on the mere fact that the delivery event decrypted successfully -- so a confirmed_at on DynastyTrust's side is genuine proof of storage, not just proof the ciphertext parsed. No outbox/retry, matching the response-channel precedent rather than the request-channel one: a missing ack only means the sender's card keeps showing unconfirmed, never a functional gap for the stored pair itself. DynastyTrust's receive side (circle-phrase-ack-channel.ts, 035_circle_phrase_delivery_confirm.sql) lives in that repo. \"Shows every time I open app\" fix (2026-08-11, same session, operator: \"They are not acknowledging and turning banner off\") -- two bugs. First, and the actual root cause: subscribeCirclePhraseDeliveries subscribes with no `since` cutoff by design, and a relay re-serves its whole matching backlog on every fresh subscribe; useCirclePhraseDeliveries.ts's old dedupe was an in-memory `seenRef` Set that reset to empty on every mount, so every historical delivery from a day of testing replayed and re-appended to the saved-banner list on every single app open (the screenshot showing \"Tapit Circle\" x6 is exactly six historical resends replaying). This is the identical root cause dismissedRequestsStore.ts documents for psbt-cosign and vault-membership (\"Even after doing the work the tab acts like its first time\") that circle-phrase never got. Fixed by reusing dismissedRequestsStore.ts directly under a new 'circle-phrase' namespace, keyed by event id, loaded once per wallet identity at mount -- deliberately NOT processedChannelEventsStore.ts, which only ever covers permanent FAILURES; a stored phrase pair has no \"still pending\" mutable state the way an unanswered request does, so persisting the SUCCESS case forever is correct and safe here (replaying an already-processed event id can never do anything different). Second: useCirclePhraseDeliveries now returns `{savedVaultNames, dismiss}` instead of a bare array, and CirclePhraseReceiver.tsx grew an actual close (×) button -- there had never been any way to acknowledge the banner within a session at all, only the (broken) hope that it would eventually stop reappearing on its own. (2026-08-13 fix, operator: \"it shows it on one side set up, but it is not showing it on the top side set up\") -- hasCirclePhrasePair's lookup by sha256(vaultDescriptor) means a vault recompile (a new descriptor) makes an already-received phrase pair silently unreachable, even though DynastyTrust's own delivery record still shows it confirmed. SignApprovalScreen.tsx was treating that as indistinguishable from \"never configured\" and silently falling back to the plain checkbox with no explanation -- a real security downgrade on a spend that was actually supposed to require the phrase ritual, the same staleness class vaultTrail.ts's diagnoseVaultTrail already handles for membership attestations. New diagnoseCirclePhrase(descriptor, vaultName) distinguishes configured / not_configured / stale (a pair exists for a vault of the SAME NAME under a different descriptor -- vault_context.vault_name travels alongside vault_descriptor on every psbt-cosign request, so this is a real, not guessed, signal) and SignApprovalScreen now shows an honest amber warning naming the stale vault and pointing at the real fix (ask the owner to resend the phrase) instead of a falsely-reassuring \"no safety phrase is set up for this vault yet.\" circlePhrase.test.ts is this feature's first test file. Same-day follow-up: the operator pushed back on the recompile explanation above (\"it doesn't change after I've set the vault up\"), correctly -- that theory was reasoned by analogy to the vaultTrail bug, never actually confirmed against this vault. CirclePhraseSection.tsx now shows each entry's short descriptor unconditionally, not only as the vaultName fallback, so a look-alike or leftover test vault (a real risk after this session's heavy iteration) can be told apart from the real one by eye instead of guessed at. Same-day, larger follow-up (operator: \"Anything that saved like that should be saved into the supabase not just the local storage of that browser... that is a terrible design if we lose stuff\") -- the registry above had no cloud mirror at all, local-IndexedDB-only, so a lost phone or a fresh browser genuinely lost every phrase pair this wallet had ever received. New remoteCirclePhraseStore.ts + 20260813000001_create_circle_phrase_backups.sql mirror the proven storage/remoteStore.ts (wallet_blobs) and mediaStore.ts patterns exactly rather than inventing a new one: pushCirclePhraseBackup encrypts the WHOLE registry with tapit-attest's encrypt() (same passphrase-derived key as the wallet blob and media backups, held in WalletContext.passphrase for exactly this class of operation) and upserts one ciphertext row per owner in circle_phrase_backups, RLS-scoped, host never sees plaintext. restoreCirclePhraseBackup merges the decrypted remote registry into local storage per VAULT ENTRY using each entry's own receivedAt as the last-write-wins timestamp, deliberately not a whole-blob overwrite -- that would risk clobbering a locally-fresher entry for a DIFFERENT vault just because the remote blob as a whole happened to be newer in aggregate. Wired into useCirclePhraseDeliveries.ts: restore runs once per unlock (best-effort, gated on cloudSync), push runs after every successful storeCirclePhrasePair (also best-effort) -- local storage stays authoritative and fully functional offline either way, matching mediaStore's non-blocking posture for its own remote mirror.",
};
