import type { FeatureManifest } from '../../shared/lib/manifest.ts';

export const manifest: FeatureManifest = {
  slug: 'connections',
  born: '2026-05-22',
  purpose:
    'Phases 5a, 5b, and 5c-ii of the Mycelium peer network (MYCELIUM_NETWORK_SPEC.md). 5a — the in-person handshake (Tier P): two wallets physically together exchange identities by QR and co-sign one relationship attestation carrying verification=in-person; both hold it. 5b — organizations and membership: an organization is itself a wallet, and a membership is a credential the organization signs about a person; memberships nest. 5c-ii — the remote handshake (Tier R, per D-09): two wallets that have never been in the same room form the same relationship attestation but with verification=remote, sent via Nostr; the initiator builds + signs + ships, the responder cosigns via the existing inbox routing, both hold the dual-signed Tier R record. ConnectionCard renders the badge by tier — accent for Tier P, neutral for Tier R — so a verifier always sees which kind of link it is. Sub-cut 2a of the per-peer chat surface roadmap (brief 2026-05-24-per-peer-chat-surface-roadmap.md section 7) adds an optional relationship leaf to the handshake envelope — family / friend / coworker / acquaintance / other — chosen by the builder in HandshakeModal and surfaced for agreement to the co-signer in the i-preview step, then rendered as a chip on ConnectionCard alongside the Tier P/R badge.',
  touches: [
    'src/features/connections/HandshakeModal.tsx',
    'src/features/connections/MembershipModal.tsx',
    'src/features/connections/createHandshake.ts',
    'src/features/connections/createHandshake.test.ts',
    'src/features/connections/createMembership.ts',
    'src/features/connections/createMembership.test.ts',
    'src/features/connections/createOrganization.ts',
    'src/features/connections/createOrganization.test.ts',
    'src/features/connections/officialsRoster.ts',
    'src/features/connections/officialsRoster.test.ts',
    'src/features/connections/openMemberRoster.ts',
    'src/features/connections/openMemberRoster.test.ts',
    'src/features/connections/verifyOpenJoinedMembership.ts',
    'src/features/connections/verifyOpenJoinedMembership.test.ts',
    'src/features/connections/OfficialsEditorModal.tsx',
    'src/features/connections/RatificationsBadge.tsx',
    'src/features/connections/MembershipChainSheet.tsx',
    'src/features/connections/ConnectionCard.tsx',
    'src/features/connections/MembershipCard.tsx',
    'src/features/connections/PeerPicker.tsx',
    'src/features/connections/extractPubkey.ts',
    'src/features/connections/ClassicConnections.tsx',
    'src/features/connections/FreshCrew.tsx',
    'src/features/connections/identicon.ts',
    'src/features/connections/IdentityChip.tsx',
    'src/features/connections/identityChipHelpers.ts',
    'src/features/connections/IdentityChip.test.ts',
    'src/features/connections/JoinOrgModal.tsx',
    'src/features/connections/PeopleTree.tsx',
    'src/features/connections/peopleTreeLayout.ts',
    'src/features/connections/peopleTreeLayout.test.ts',
    'src/features/connections/familyUnit.ts',
    'src/features/connections/familyUnit.test.ts',
  ],
  depends_on: ['wallet-core', 'qr', 'cosigning', 'anchoring', 'theme', 'governance'],
  pause_safe: true,
  removal_safe: false,
  monetizable: false,
  notes:
    'A handshake is a relationship-kind attestation; a membership is a credential-kind attestation — no new tapit-attest kinds. The modals reuse QrShow / QrScanModal and the cosigning parseEnvelope + mergeSignatures helpers. wallet-core/HomeScreen.tsx imports both modals and both cards, so removal_safe is false. The handshake is co-signed (3 QR transmissions for Tier P); a membership is one-directional (2 QR transmissions — only the issuing organization signs). Governance direction: from Phase 8 Phase A onward, the org\'s authorization-rule substrate lives in src/features/governance/ as org-agnostic primitives (AuthRule + JoinPolicy + findAuthRule + listAuthRules + proveAuthorization + AuthorizedByPayload encode/decode/build + evaluateJoinPolicy); this connections feature consumes them via the declared `governance` dependency and adds the org-specific glue — selfDeclareOrganization wraps buildAuthSubtree at declaration time, verifyOrgAuthorization composes verifyDisclosureProof with findAuthRule on the signer-side verifier, MembershipModal threads buildAuthorizedByPayload through the org-issued credential, openMemberRoster adds the periodic roster envelope shape, and verifyOpenJoinedMembership (Phase E4 cut 1) composes findAuthRule + isJoinRule + evaluateJoinPolicy + the roster reader to verify open-joined self-memberships under the Option 3 hybrid substrate. Tier R remote handshakes (5c-ii) reuse the same envelope shape with verification=remote and travel through the Mycelium transport; the responder side reuses 5c-i-ε auto-routing (1-sig → cosign-witness; 2-sig → absorb). 5b-org-i adds createOrganization — a self-signed credential-kind attestation lets a wallet declare itself an organization; Home flips to org-mode rendering on the Identity tab; Settings exposes the declaration form (one-way for now). 5b-org-ii adds the officials roster — a second self-signed credential names the organization current officers, with publishOfficialsRoster snapping a new full roster envelope on every edit (history preserved; latest by issued_at wins). OfficialsEditorModal handles add / remove inline; readOfficials + findLatestOfficialsRoster are reusable by future cuts (ratifications view, verifier flows). 5b-org-iii ships countRatifications + RatificationsBadge — the badge renders N of M ratifications when the viewer has access to the org roster (org-side always; member-side only if the operator holds a copy of the org roster). Tone scales by progress (neutral / amber / emerald). Member-side rendering silently omits the badge when no roster is available so the card stays honest about what can be locally verified. 5b-org-iv ships walkOrgChain + MembershipChainSheet — tapping a MembershipCard walks the nesting chain upward through local holdings (you are a member of X; X is a member of Y; Y of Z) and renders the stack. When the walker runs out of locally-held data it labels the chain truncated so the operator stays honest about visibility. Read-only; reusable from any context that wants to walk the structure. Phase 8 Phase A (Tapscript-style org authorization tree, brief 2026-05-25-tapscript-style-org-authorization-tree-roadmap.md) adds AuthRule + findAuthRule + listAuthRules + proveAuthorization on top of createOrganization.ts. selfDeclareOrganization gains an optional authRules parameter; rules become a sub-branch named auth in the claim tree, each rule encoded as a leaf with canonical JSON of {threshold, eligible} as the value. proveAuthorization wraps the shipped disclosureProof primitive to produce a cross-envelope authorization proof that Phase B verifier will consume on org-issued credentials. Validation throws on duplicate action, threshold < 1, non-integer threshold, threshold > eligible-set size — caught at creation time so unsignable rules never reach storage. createOrganization.test.ts ships 15 tests covering the validation gates and the disclosure-proof round-trip including the cross-envelope tamper-detection case. Phase 8 Phase B adds the verifier side: AuthorizedByPayload type + encodeAuthorizedBy / decodeAuthorizedBy / buildAuthorizedByPayload helpers + verifyOrgAuthorization(envelope, knownOrgs) → {authorized, reason, eligibleCount?, thresholdRequired?}. An org-issued credential carries authorized_by as a top-level leaf whose value is JSON of {org_identity, action, proof: DisclosureProofBundle}; the envelope signature covers the proof too so it cannot be detached and swapped. verifyOrgAuthorization looks up the org self-declaration in knownOrgs, runs verifyDisclosureProof, confirms the proof digest equals the self-declaration envelopeId (the cross-envelope binding), confirms the disclosed leaf name matches the claimed action, decodes the rule, and counts distinct eligible-signer signatures against threshold. createOrganization.test.ts grows to 34 tests covering all four forgery classes flagged in the Phase A close-out (leaf-value tampered, wrong-org-binding via same-subject-different-digest, tampered sibling-hash path, tampered meta-fields) plus action-claim mismatch, plus the standard happy-path / missing-org / malformed / threshold-not-met / eligible-list-mismatch refusal cases. Post-Phase-B extraction cut: the Phase A + B auth-rule and authorized_by helpers (AuthRule type, encode/decode/build, findAuthRule, listAuthRules, proveAuthorization, AuthorizedByPayload, encode/decode/buildAuthorizedByPayload, OrgAuthorizationResult) moved to src/features/governance/authRule.ts and are re-exported from createOrganization.ts for back-compat with existing import sites. A new pure buildOrgSelfDeclarationDraft helper follows the buildHandshakeDraft pattern in createHandshake.ts — production selfDeclareOrganization wraps it; tests call it directly to bypass the async hold/anchor pipeline without duplicating the encoding logic. createOrganization.ts drops from 726 to 533 lines, restoring file-size headroom for Phase C UI work. Phase 8 Phase C cut 3 caller-wiring closes the org-issuance loop end-to-end: buildMembershipDraft in createMembership.ts gains an optional authorizedBy parameter that bakes an authorized_by top-level leaf carrying the disclosure proof of the issuance rule, and MembershipModal threads it through when the operator has self-declared as an org (looked up via findOwnOrgDeclaration + findAuthRule for the routine_issuance action). The org signature covers the proof leaf so it cannot be detached and swapped; verifyOrgAuthorization downstream reads it to confirm threshold and eligible-signer set match the org declaration. When the routine_issuance rule has threshold > 1, MembershipModal surfaces an amber "Request co-signs from eligible signers" banner on the issue-show step that opens a lazy-loaded CosignRequestModal in orgContext mode (same lazy chunk JournalDetail and PromoteRouter already share, so no static-import bloat in HomeScreen). createMembership.test.ts ships 4 round-trip tests: back-compat draft has no authorized_by leaf; threshold=1 founder-only draft passes verifyOrgAuthorization; threshold=2 single-signed draft fails with threshold-not-met; threshold=2 dual-signed draft passes once the second eligible signer co-signs the same envelope. All four gates green; no bundle-budget bumps required. Phase 8 Phase E2 lands joiner-side self-membership inside the same createMembership.ts so the two credential shapes (org-issued membership, joiner-signed self-membership) stay close enough that consumers see them side-by-side: a new `buildSelfMembershipDraft(joinerIdentity, orgId, orgName)` produces an unsigned credential-kind attestation with `credential_type: self_membership` plus `org_id` / `org_name` / `joined_at` / `requested_at` leaves all set at draft time (the two timestamp leaves stay identical under the Option 2 substrate; Phase E3/E4 sharpens the distinction if the org publishes a roster that ratifies a later acceptance time); `isSelfMembership` is the predicate complement to `isMembership` — mutually exclusive by credential_type so consumers never confuse the two; `readSelfMembership` lifts the signed leaves into a SelfMembershipView for downstream display; `receiveSelfMembership` is the Phase E2 acceptor placeholder that holds + anchors an incoming self-membership locally so the envelope is not lost between Phases (Phase E3 layers join-policy evaluation and pending-roster buffering on top of this same hook). HomeScreen.tsx imports `receiveSelfMembership` and gains an `acceptSelfMembership` branch in routeInbox; envelopeRoute.ts adds the `self-membership-receive` action so the inbox + ScanEnvelopeModal both dispatch correctly. createMembership.test.ts grows from 4 to 11 tests covering the new builder, predicate, reader, and the integrity-gate behaviour of receiveSelfMembership (storage round-trip belongs to Phase E3 because anchorQueue.upsert hits IndexedDB which jsdom does not ship). File grows from 122 to ~225 lines — well under the 400-line soft warn and 800-line hard limit, so no extraction. No new bundle budgets required — buildSelfMembershipDraft has no production caller until Phase E4 UI lands, and receiveSelfMembership ships inside the existing createMembership chunk already in the HomeScreen import graph. Phase 8 Phase E3 cut 1 (Option 3 hybrid substrate per the open-joining brief) replaces the Phase E2 placeholder behavior of receiveSelfMembership with structural join-policy gating: the acceptor now requires the receiving wallet to hold its own org self-declaration (the caller — HomeScreen.acceptSelfMembership — passes it in alongside the wallet\'s holdings), looks up the `join` action rule via findAuthRule, type-narrows it via isJoinRule, and runs the new evaluateJoinPolicy helper in src/features/governance/. Three of the six policy kinds (open / allow_list / deny_list) accept or reject purely from the envelope\'s joiner pubkey; the other three (requires_handshake / requires_credential / requires_vouch) reject with a reason naming Phase E4 as the milestone that adds joiner-side proof attachment. Reject reasons surface through the thrown Error message so a future UI surface can show the org operator why a join request did not land. createMembership.ts grows from 223 to 251 lines (still under the 400-line soft warn) and createMembership.test.ts grows by three scenarios covering the new gates (org-without-join-rule rejection, allow/deny verdict surfacing in error message, Phase-E4-deferred kind rejection). HomeScreen.acceptSelfMembership early-returns with a console.warn when the receiving wallet has not declared itself as an org — a self-membership routed to a personal wallet has no business being accepted into holdings, so the silent short-circuit is the honest answer. Phase 8 Phase E3 cut 2 ships the open-member roster substrate in src/features/connections/openMemberRoster.ts — the same envelope-shape pattern createOrganization\'s officials-roster uses: credential-kind attestation, subject = org pubkey, credential_type leaf = `open_member_roster`, members leaf = canonical JSON of {member_id, joined_at, self_membership_envelope_id} entries sorted ascending by joined_at then member_id, issued_at leaf at publish time. Six exports: acceptedSelfMemberships (all self-memberships in holdings addressed to the org, sorted), pendingSelfMemberships (delta — accepted minus already-on-latest-roster), buildOpenMemberRosterDraft (pure unsigned builder), publishOpenMemberRoster (signs + holds + anchors the full accepted set), findLatestOpenMemberRoster (latest-by-issuedAt selection, same shape as findLatestOfficialsRoster), readOpenMemberRoster (shape-tolerant member-list parser that drops malformed entries individually). 14 tests covering predicate, filtering by org_id, sort order, entry shape, latest-wins selection, delta computation, empty + already-published edge cases. publishOpenMemberRoster goes through wallet.hold + anchorQueue.upsert which jsdom does not back; tests exercise the pure builders + signed wallet.hold directly so the same envelope shape the production path emits is verified without the IndexedDB layer. Phase E4 verifier consults findLatestOpenMemberRoster as the Option 1 path and the auth-tree walk via findAuthRule as the Option 2 path; the hybrid substrate accepts whichever proof verifies. UI work (a "Publish roster" button + a pending-delta surface on the Members view) lands in Phase E4 alongside the verifier. Phase 8 Phase E4 cut 1 ships src/features/connections/verifyOpenJoinedMembership.ts — the open-joined-membership verifier function under the Option 3 hybrid substrate. Pure composer over the already-shipped pieces: findAuthRule + isJoinRule from governance/authRule.ts, evaluateJoinPolicy from governance/evaluateJoinPolicy.ts, isOpenMemberRoster + readOpenMemberRoster from openMemberRoster.ts, isSelfMembership from createMembership.ts, isOrganizationSelfDeclaration from this file. The single export verifyOpenJoinedMembership(envelope, orgSelfDecl, currentRoster?) returns {valid, reason, proofPath} where proofPath names \'roster\' on Option 1 success, \'auth_tree\' on Option 2 success, or \'none\' on rejection. Structural gates run before either path: envelope must be a self-membership credential, orgSelfDecl must be an organization self-declaration, envelope\'s org_id leaf must match orgSelfDecl.subject (case-insensitive), envelope must carry a signature from its subject (the joiner), orgSelfDecl must carry a signature from its subject (the org). Then the roster path is tried first (cheaper, single artifact): roster must be subject-bound to the org, signed by the org, and contain an entry where member_id matches envelope.subject AND self_membership_envelope_id matches envelopeId(envelope). If the roster path rejects, the auth-tree path runs via findAuthRule(orgSelfDecl, \'join\') + isJoinRule narrow + evaluateJoinPolicy(rule.policy, envelope, []). The three list-checking policy kinds (open / allow_list / deny_list) accept or reject from the envelope alone per Phase E3 cut 1\'s evaluator contract; the three proof-required kinds (requires_handshake / requires_credential / requires_vouch) reject with reason naming Phase E4 cut 2 as the milestone that adds joiner-side proof attachment + reading. Both paths rejecting yields a consolidated reason naming both attempted paths. 19 new tests covering: structural-gate rejections (non-self-membership envelope, non-org-decl orgSelfDecl, org_id mismatch, unsigned envelope), auth-tree path acceptance (open / allow_list-present / deny_list-absent), auth-tree path rejection (allow_list-absent / deny_list-present / three proof-required-kind deferrals / no-join-rule), roster path acceptance (signed roster names the joiner), roster path fall-through to auth tree (roster missing the joiner / roster bound to different org), hybrid override (roster wins when auth-tree rejects), both-paths-rejecting case, and case-insensitive org_id binding. The verifier is pure — no I/O, no wallet calls — so tests build envelopes via the existing buildSelfMembershipDraft + buildOrgSelfDeclarationDraft + buildOpenMemberRosterDraft drafts and sign with throwaway Wallet.generate() pairs. Phase E4 cut 2 will extend this verifier with the proof-required-policy paths (handshake / credential / vouch reading proof leaves off the envelope) without changing the public signature; Phase E4 cut 3 wires the UI (org-creation policy picker, Members view rendering the chronological roster, publish-roster button + pending-delta surface, any-wallet Profile join-an-org flow). Phase 8 Phase E4 cut 2 extends both the joiner-side builder and the org-side evaluator to support the three proof-required policy kinds (requires_handshake / requires_credential / requires_vouch) under the operator-locked DisclosureProof-plus-cosigs attachment shape. buildSelfMembershipDraft now accepts an optional fourth `proofs` parameter (SelfMembershipProofs = {handshake_proof?, credential_proof?}); each supplied proof is a tapit-attest DisclosureProofBundle that the builder JSON-stringifies into a top-level leaf on the self-membership envelope so the joiner\'s own signature covers the proof leaf — it cannot be detached or swapped after signing, same Tapscript-style reveal-one-leaf discipline the Phase A/B authorized_by leaf pattern established. Vouch needs no new leaf because cosignatures already ride envelope.signatures[]. Two paired reader helpers readHandshakeProof + readCredentialProof JSON.parse the leaf back into a DisclosureProofBundle with a lightweight shape gate (returns null on absent or malformed leaves so verification-time errors surface as honest reject reasons rather than thrown exceptions). evaluateJoinPolicy in src/features/governance/ flips the three Phase-E3-deferred returns into real verification: requires_handshake reads handshake_proof, runs verifyDisclosureProof, confirms meta.kind=relationship + the disclosed leaf is a non-empty verification leaf + signatures include the joiner AND at least one pubkey from policy.with_any_of; requires_credential reads credential_proof, runs verifyDisclosureProof, confirms meta.kind=credential + meta.subject=joiner + the disclosed credential_type matches policy.credential_type + (when issuer is named) signatures include that issuer; requires_vouch derives the org\'s known-member set inline from holdings (subjects of self-memberships whose org_id leaf names this org, plus member_id leaves of memberships issued by this org — predicate logic duplicated here rather than importing from connections so governance stays free of reverse dependencies on its consumers), counts cosigners on selfMembership.signatures that are in that set minus the joiner\'s own signature, and accepts when the count meets policy.from_any_member_count. The verifyOpenJoinedMembership public signature stays stable per the prior letter — cut 2 work is entirely internal to the evaluator; the verifier-side path (which passes empty holdings) naturally rejects vouch with "no known members" because verifier-side callers without member context cannot evaluate that policy kind, which is the honest answer (vouch joiners go through the Option 1 roster path or org-side receive in production). evaluateJoinPolicy.test.ts grows from 13 to 21 tests covering: missing-proof-leaf reject, malformed-JSON reject, happy-path acceptance for handshake / credential (with and without named issuer) / vouch (member-set derived from self-memberships AND from org-issued memberships), wrong-envelope-kind reject (credential proof used as handshake proof), wrong-credential-type reject, wrong-subject reject, wrong-issuer reject, insufficient-voucher-count reject. createMembership.test.ts grows from 18 to 22 tests covering the proof-attachment builder (handshake_proof + credential_proof leaves round-trip through readers, back-compat five-field shape preserved when proofs omitted, readers null-safe on absent and malformed leaves) and the receiveSelfMembership happy-path now reaches the wallet.hold boundary on a requires_credential policy when a valid credential_proof rides along. verifyOpenJoinedMembership.test.ts: the three Phase-E4-deferred tests flip to assert the new concrete reject reasons (no handshake_proof leaf / no credential_proof leaf / no known members in holdings). The on-wire envelope shape now locked: handshake_proof and credential_proof are top-level claim-tree leaves carrying canonical JSON of the DisclosureProofBundle; cosignatures for vouch ride signatures[] as before. Phase E4 cut 3 wires the UI on top of this substrate without changing the on-wire shape. Post-Phase-E4-cut-2 extraction: the officials roster + ratifications helpers (Official, RatificationSummary, isOfficialsRoster, readOfficials, findLatestOfficialsRoster, countRatifications, publishOfficialsRoster) moved from createOrganization.ts to src/features/connections/officialsRoster.ts as a sibling module mirroring openMemberRoster.ts on the membership-direction half. createOrganization.ts drops from 546 to 366 lines (back under the 400-line soft warn band), retaining the self-declaration surface + the chain walker (walkOrgChain). The five consumer sites (HomeScreen.tsx, OrgIdentitySections.tsx, MembershipCard.tsx, RatificationsBadge.tsx, OfficialsEditorModal.tsx) updated to import directly from \'./officialsRoster.ts\' (or \'../connections/officialsRoster.ts\' from the wallet-core side); no back-compat re-export from createOrganization.ts because the dependency graph is cleaner with direct imports and the five-file churn is bounded. The officials-vs-open-member parallel is now legible at the filesystem level: officialsRoster.ts owns the governance-direction roster (who runs the org), openMemberRoster.ts owns the membership-direction roster (who has joined the org), and createOrganization.ts owns the org declaration that points to both. The extraction also picked up the test-coverage gap createOrganization.test.ts had never closed — officialsRoster.test.ts ships 16 focused tests covering: isOfficialsRoster predicate (true for roster, false for other credential kinds), readOfficials (round-trip canonical JSON, drops malformed pubkey entries individually, returns empty on missing leaf, returns empty on non-JSON leaf, lowercases pubkeys), findLatestOfficialsRoster (latest-by-issued_at selection in either input order, null when no roster matches the named org, skips rosters the org never signed, null on holdings with no rosters at all), countRatifications (counts officials whose signatures appear, null on empty officials, pubkey-prefix fallback when an official has no name), and publishOfficialsRoster\'s validation gate (throws on invalid-hex pubkey BEFORE the IndexedDB layer the full publish wrapper would otherwise hit). The publish wrapper\'s storage round-trip stays unverified for the same reason buildOpenMemberRosterDraft\'s does: anchorQueue.upsert hits IndexedDB which jsdom does not back. Tests cover the same envelope shape the production path emits one method-call earlier in the chain. Phase 8 Phase E4 cut 3 wires the UI on top of the substrate. Three threads landed in one session: the org-creation join-policy picker (sibling JoinPolicyPicker.tsx in settings/, lazy-loaded alongside the existing OrgRulesEditor pattern; six policy-kind sub-forms with per-kind validation that mirrors buildAuthSubtree\'s contract), the org-mode Members + publish-roster surface (OrgIdentitySections.tsx gains a Joined-members section pulling acceptedSelfMemberships in chronological join order with a pending-delta amber chip wired to publishOpenMemberRoster), and the any-wallet Join-an-org modal (new JoinOrgModal.tsx — the novel piece). JoinOrgModal owns the joiner-side disclosure-proof-construction flow that the Phase E4 cut 2 substrate locked: paste-or-scan an org self-declaration via parseEnvelope + isOrganizationSelfDeclaration, read the declared join-policy via findAuthRule + isJoinRule, render the policy in plain language via a kind-dispatched describePolicy helper, (for requires_handshake / requires_credential) walk the joiner through picking a proof-eligible attestation from their holdings via findHandshakeProofCandidates + findCredentialProofCandidates (both filter against the policy + the joiner\'s own subject so the proof discloses an attestation issued to them, not a held copy of someone else\'s), package the chosen attestation as a tapit-attest disclosureProof bundle (over the verification leaf for handshake, the credential_type leaf for credential), bake the bundle into the self-membership envelope via buildSelfMembershipDraft\'s Phase E4 cut 2 proofs parameter, sign with the joiner\'s wallet, then offer QR + Mycelium delivery so the org-side acceptor (HomeScreen.acceptSelfMembership) can ingest the envelope through the existing self-membership-receive routing. Steps: \'find\' → \'review\' → \'proof\' (only when proof required) → \'send\' → \'done\'. The vouch policy gets a plain-prose notice on the send step explaining that cosignatures from existing members must be collected before the org will accept — the cosig flow itself is not yet wired through JoinOrgModal but the joiner-signed envelope is a valid starting point for the existing cosigning surface. HomeScreen.tsx adds a "+ Join an org" button next to "+ Membership" on the Identity-tab Memberships header; the modal is React.lazy so its body chunk (4KB gz, JoinOrgModal- prefix) only loads when the operator opens the flow. SettingsScreen + HomeScreen both hit the 800-line hard limit with the E4 cut 3 additions, so two extractions landed in the same session: src/features/settings/OrgDeclarationSection.tsx encapsulates the org-creation form (orgName + orgRules + joinPolicy state machine) so SettingsScreen drops from 823 to 656 lines; src/features/wallet-core/useOpenMemberRosterControls.ts encapsulates the joinedMembers + pendingMembers + publishing flag + publish callback so HomeScreen drops from 824 to 797 lines. The on-wire envelope shape stays unchanged from cut 2 — cut 3 is purely about exposing the substrate to operator-visible surfaces. No new bundle-budget overages beyond the explicit JoinPolicyPicker chunk (~2.2KB gz), JoinOrgModal chunk (~4KB gz), the HomeScreen bump for the openMemberRoster static imports (+1KB), the SettingsScreen bump for the OrgDeclarationSection extraction (+0.5KB), and the CSS bump for the new Tailwind utilities (+0.5KB). Vouch loop-back polish (2026-05-27) closes the joiner-side return leg the cut-3 vouch sub-flow had left dangling. JoinOrgModal now wallet.holds the freshly-signed self-membership before the send step when policy.kind is requires_vouch (only that kind — other policies remain ephemeral on the joiner side until the org accepts) so AbsorbCosignModal\'s envelopeId-based lookup can locate the held copy and merge a returned vouch into it. A new liveSelfMembership useMemo reads the latest version from holdings on each render so the QR + Mycelium send + the CosignRequestModal mount all ship the cosigned bundle (not the stale 1-sig copy from component state) once vouches accumulate. The send-step vouch banner renders a progress chip "X of Y vouches collected" tone-scaled amber-to-emerald against the policy.from_any_member_count threshold; the chip\'s copy + the collect-vouches button label flip when the threshold is met so the operator sees a clear "ready to send" signal. The routing-substrate side of this loop landed in the transport feature: routeFor now takes an optional receiverPubkey and routes a peer-cosigned self-membership back to absorb-cosign on the joiner\'s wallet when receiver IS the envelope subject AND signatures.length > 1. JoinOrgModal grows from 616 to 670 lines (already over the 400-line soft warn from cut 3 — the next JoinOrgModal touch should plan an extraction; natural candidate is the proof-candidate finders findHandshakeProofCandidates + findCredentialProofCandidates which are pure-logic helpers that could move to a sibling module). No bundle-budget changes.',
};
