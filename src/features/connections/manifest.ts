import type { FeatureManifest } from '../../shared/lib/manifest.ts';

export const manifest: FeatureManifest = {
  slug: 'connections',
  born: '2026-05-22',
  purpose:
    'Phases 5a, 5b, and 5c-ii of the Mycelium peer network (MYCELIUM_NETWORK_SPEC.md). 5a — the in-person handshake (Tier P): two wallets physically together exchange identities by QR and co-sign one relationship attestation carrying verification=in-person; both hold it. 5b — organizations and membership: an organization is itself a wallet, and a membership is a credential the organization signs about a person; memberships nest. 5c-ii — the remote handshake (Tier R, per D-09): two wallets that have never been in the same room form the same relationship attestation but with verification=remote, sent via Nostr; the initiator builds + signs + ships, the responder cosigns via the existing inbox routing, both hold the dual-signed Tier R record. ConnectionCard renders the badge by tier — accent for Tier P, neutral for Tier R — so a verifier always sees which kind of link it is. Sub-cut 2a of the per-peer chat surface roadmap (brief 2026-05-24-per-peer-chat-surface-roadmap.md section 7) adds an optional relationship leaf to the handshake envelope — family / friend / coworker / acquaintance / other — chosen by the builder in HandshakeModal and surfaced for agreement to the co-signer in the i-preview step, then rendered as a chip on ConnectionCard alongside the Tier P/R badge.',
  touches: [
    'src/features/connections/HandshakeModal.tsx',
    'src/features/connections/MembershipModal.tsx',
    'src/features/connections/createHandshake.ts',
    'src/features/connections/createHandshake.test.ts',
    'src/features/connections/createMembership.ts',
    'src/features/connections/createOrganization.ts',
    'src/features/connections/createOrganization.test.ts',
    'src/features/connections/OfficialsEditorModal.tsx',
    'src/features/connections/RatificationsBadge.tsx',
    'src/features/connections/MembershipChainSheet.tsx',
    'src/features/connections/ConnectionCard.tsx',
    'src/features/connections/MembershipCard.tsx',
    'src/features/connections/PeerPicker.tsx',
    'src/features/connections/extractPubkey.ts',
    'src/features/connections/ClassicConnections.tsx',
    'src/features/connections/FreshCrew.tsx',
    'src/features/connections/identicon.ts',
  ],
  depends_on: ['wallet-core', 'qr', 'cosigning', 'anchoring', 'theme', 'governance'],
  pause_safe: true,
  removal_safe: false,
  monetizable: false,
  notes:
    'A handshake is a relationship-kind attestation; a membership is a credential-kind attestation — no new tapit-attest kinds. The modals reuse QrShow / QrScanModal and the cosigning parseEnvelope + mergeSignatures helpers. wallet-core/HomeScreen.tsx imports both modals and both cards, so removal_safe is false. The handshake is co-signed (3 QR transmissions for Tier P); a membership is one-directional (2 QR transmissions — only the issuing organization signs). Tier R remote handshakes (5c-ii) reuse the same envelope shape with verification=remote and travel through the Mycelium transport; the responder side reuses 5c-i-ε auto-routing (1-sig → cosign-witness; 2-sig → absorb). 5b-org-i adds createOrganization — a self-signed credential-kind attestation lets a wallet declare itself an organization; Home flips to org-mode rendering on the Identity tab; Settings exposes the declaration form (one-way for now). 5b-org-ii adds the officials roster — a second self-signed credential names the organization current officers, with publishOfficialsRoster snapping a new full roster envelope on every edit (history preserved; latest by issued_at wins). OfficialsEditorModal handles add / remove inline; readOfficials + findLatestOfficialsRoster are reusable by future cuts (ratifications view, verifier flows). 5b-org-iii ships countRatifications + RatificationsBadge — the badge renders N of M ratifications when the viewer has access to the org roster (org-side always; member-side only if the operator holds a copy of the org roster). Tone scales by progress (neutral / amber / emerald). Member-side rendering silently omits the badge when no roster is available so the card stays honest about what can be locally verified. 5b-org-iv ships walkOrgChain + MembershipChainSheet — tapping a MembershipCard walks the nesting chain upward through local holdings (you are a member of X; X is a member of Y; Y of Z) and renders the stack. When the walker runs out of locally-held data it labels the chain truncated so the operator stays honest about visibility. Read-only; reusable from any context that wants to walk the structure. Phase 8 Phase A (Tapscript-style org authorization tree, brief 2026-05-25-tapscript-style-org-authorization-tree-roadmap.md) adds AuthRule + findAuthRule + listAuthRules + proveAuthorization on top of createOrganization.ts. selfDeclareOrganization gains an optional authRules parameter; rules become a sub-branch named auth in the claim tree, each rule encoded as a leaf with canonical JSON of {threshold, eligible} as the value. proveAuthorization wraps the shipped disclosureProof primitive to produce a cross-envelope authorization proof that Phase B verifier will consume on org-issued credentials. Validation throws on duplicate action, threshold < 1, non-integer threshold, threshold > eligible-set size — caught at creation time so unsignable rules never reach storage. createOrganization.test.ts ships 15 tests covering the validation gates and the disclosure-proof round-trip including the cross-envelope tamper-detection case. Phase 8 Phase B adds the verifier side: AuthorizedByPayload type + encodeAuthorizedBy / decodeAuthorizedBy / buildAuthorizedByPayload helpers + verifyOrgAuthorization(envelope, knownOrgs) → {authorized, reason, eligibleCount?, thresholdRequired?}. An org-issued credential carries authorized_by as a top-level leaf whose value is JSON of {org_identity, action, proof: DisclosureProofBundle}; the envelope signature covers the proof too so it cannot be detached and swapped. verifyOrgAuthorization looks up the org self-declaration in knownOrgs, runs verifyDisclosureProof, confirms the proof digest equals the self-declaration envelopeId (the cross-envelope binding), confirms the disclosed leaf name matches the claimed action, decodes the rule, and counts distinct eligible-signer signatures against threshold. createOrganization.test.ts grows to 34 tests covering all four forgery classes flagged in the Phase A close-out (leaf-value tampered, wrong-org-binding via same-subject-different-digest, tampered sibling-hash path, tampered meta-fields) plus action-claim mismatch, plus the standard happy-path / missing-org / malformed / threshold-not-met / eligible-list-mismatch refusal cases. Post-Phase-B extraction cut: the Phase A + B auth-rule and authorized_by helpers (AuthRule type, encode/decode/build, findAuthRule, listAuthRules, proveAuthorization, AuthorizedByPayload, encode/decode/buildAuthorizedByPayload, OrgAuthorizationResult) moved to src/features/governance/authRule.ts and are re-exported from createOrganization.ts for back-compat with existing import sites. A new pure buildOrgSelfDeclarationDraft helper follows the buildHandshakeDraft pattern in createHandshake.ts — production selfDeclareOrganization wraps it; tests call it directly to bypass the async hold/anchor pipeline without duplicating the encoding logic. createOrganization.ts drops from 726 to 533 lines, restoring file-size headroom for Phase C UI work.',
};
