import type { FeatureManifest } from '../../shared/lib/manifest.ts';

export const manifest: FeatureManifest = {
  slug: 'move-chain',
  born: '2026-09-03',
  purpose:
    "The reusable mechanism behind 'Beat the HODL Machine' and any idea like it: an append-only, hash-linked chain of signed 'move' attestations rooted at a genesis, broadcast live over Nostr so anyone can watch it happen and verify it. A person places a rock and then buys and sells chunks; every move is a signed, ordered, tamper-evident record that walks all the way back to where the session started with no gap. It sits on the SAFE side of the attestation-vs-spend wall: a move expresses INTENT ('bought $250 of BTC at $70,000') and can never move a coin — it is an ordinary tapit-attest journal attestation, created and signed through the Wallet, chained on the library's own envelopeId, verified with the library's own verifyEnvelope. No new crypto is invented; different product ideas ride the same primitive by supplying their own move payload.",
  touches: [
    'src/features/move-chain/moveChain.ts',
    'src/features/move-chain/moveChain.test.ts',
    'src/features/move-chain/truthScore.ts',
    'src/features/move-chain/truthScore.test.ts',
    'src/features/move-chain/chainVerify.ts',
    'src/features/move-chain/chainVerify.test.ts',
    'src/features/move-chain/manifest.ts',
    'src/shared/lib/base64url.ts',
    'src/features/transport/moveChannel.ts',
    'src/features/transport/moveChannel.test.ts',
    'src/features-registry.ts',
  ],
  depends_on: ['transport'],
  pause_safe: true,
  removal_safe: true,
  monetizable: false,
  notes:
    "Cut 1 (2026-09-03): the pure primitive + the public broadcast channel, no UI surface yet (the arena screen is a later cut, and until something imports these modules they add nothing to the bundle). moveChain.ts is PURE and fully tested (14 cases): buildMoveDraftInput shapes a move's DraftInput (chain metadata seq/prev live in the claim ALONGSIDE the idea's payload, nested under a `move` branch so a payload key can never collide, and both are covered by the signature); moveLink(att) = envelopeId(att) is the content address the next move chains on; readMoveMeta reads seq/prevHash/payload back; verifyMoveChain is all-or-nothing (every move validly signed BY ITS OWN SUBJECT IDENTITY so no one can claim another's chain, same owner as genesis, seq strictly 0,1,2…, prevHash === moveLink of the prior, genesis seq 0 / prev '') — a hole, reorder, edit, or identity-forgery all read invalid; orderMoves reassembles arrival-order into chain order. The broadcast channel is src/features/transport/moveChannel.ts on its OWN public wire kind MOVE_EVENT_KIND = 9584 (a sibling to envelope 9573 / liveness 9575 / sign-in 9582-9583) — UNENCRYPTED and unaddressed by design because a move is world-readable, the whole point being that a verifier anywhere can reassemble and check a chain. publishMove wraps a signed move as a Nostr event (content = the move JSON, tags = topic `t` + seq + the chain-root `e` on non-genesis moves) via buildEvent (key never leaves the wallet — signs through wallet.signDigest). subscribeMoves verifies the outer event, parses + assertWellFormed's the inner move, re-verifies the inner signature with verifyEnvelope, and BINDS the broadcaster to the author (the event pubkey must be a valid signer of the move it carries) so a relay or stranger cannot inject a move under a foreign name; every failure drops silently, dedup by event id. Tested (7 cases) against a fake Transport that records publishes and replays events — the whole channel logic is exercised EXCEPT the real relay websocket, which the sandbox can't reach: a live broadcast round-trip (watch the wallet's npub / the topic feed in a Nostr client) is owed on device. v1 binds a chain to one un-rotated identity (subject === a valid signer); honoring a mid-chain key rotation via the existing succession primitive is a later refinement. NOT built yet: the arena UI surface, and the genesis-of-chain wiring that ties a move chain to a donation/entry (the payment-genesis idea from the design chats) — this cut is the spine those ride. CUT 2 (2026-09-03): src/features/move-chain/truthScore.ts — 'your math shows the truth'. The pure, tested scorer for the WHOLE-COIN, all-in-or-all-out game (operator rule 2026-09-03: you start holding one whole bitcoin, and your only moves are sell the whole coin, buy the whole coin back — sell, buy back, sell, buy back). simulateWholeCoin replays the switches against the stamped prices, charges friction on EVERY leg, and reports coin count now vs the HODL ball (a fixed 1.0), the coins given up to friction (an honest counterfactual: the same moves run frictionless minus the real run), each sell-buyback round's contribution, and — while holding cash — the price you must buy back at to beat the ball. An out-of-turn move (sell while in cash, buy while in coin) is skipped and flags the result not-well-formed rather than corrupting the count. readWholeCoinMoves bridges a verified move chain into the scorer, skipping the genesis 'start' move. 11 tests including the carried-to-the-top run (two good round trips = four coins), friction, the open-sell threshold, and the bridge. STAKE DECISION (operator, 2026-09-03): the donation/stake is NOT a gate and NOT part of the math — anyone plays for free and sees they can't beat the ball; the stake is only how seriously the world takes a claim (a dollar is taken as a dollar's worth of seriousness; a real player stakes more). So the scorer is stake-agnostic (coin count vs 1.0), and any credibility/stake layer sits beside it, never inside it. CUT 3 (2026-09-10): src/features/move-chain/chainVerify.ts — lets a stranger verify a WHOLE chain, genesis through the latest move, not just one disclosed move (the operator: \"I was picturing the whole chain of it could be verified and people learn how it's possible. Genesis and each one built on the last hash.\"). Unlike disclosure's pruned Merkle proofs (built for hiding most of a claim), a move has nothing sensitive to hide, so the bundle is just the chain's own full, already-signed attestations — no pruning needed. buildChainVerifyUrl mints {json, verifyUrl, urlIsInline}, same shape as disclosure's buildVerifyUrl, at a looser 5KB inline-URL budget (a whole chain runs ~800-900B JSON per move, heavier than a single pruned disclosure proof, and this rides in a Nostr note rather than a QR code or iMessage preview). parseChainProofBundle detects + shape-checks a pasted/decoded bundle without throwing (returns null for anything that isn't one), and describeChainSteps re-derives the SAME per-move checks verifyMoveChain already runs (verifyEnvelope, moveLink comparison) purely to narrate each step for display — verifyMoveChain itself stays the one trusted verdict; describeChainSteps is never a second source of truth. Consumed by disclosure's /verify page (ChainVerifyResult.tsx, lazy-loaded there since it's the less-common path) and by arena's share-text builder (buildChainVerifyUrl replaces the old per-move disclosure-proof link). base64UrlEncode/Decode were hoisted to src/shared/lib/base64url.ts out of disclosure's buildVerifyUrl.ts + VerifyProofScreen.tsx so this became the third caller without becoming a third private copy. 5 tests: mint + round-trip, non-chain input returns null rather than throwing, a genuine chain reports every step valid, and a tampered chain (a foreign move substituted in) correctly flags the broken link.",
};
