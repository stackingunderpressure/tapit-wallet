# carpenter-state-for-foreman — Phase E3 cuts 1 and 2 under Option 3 hybrid

> PFOR-012 structured operational state. Written 2026-05-26 early afternoon at session close-out. Two code commits plus one close-out commit landed on the dispatch branch `claude/multisig-orgs-status-jiLwm` this session. Branch tip pushed to main per PFOR-016 so the Stop-hook gate clears. All four gates green throughout. 202 of 202 tests passing (was 176 on session start; +26).

**Operator-mode note:** Dispatched session via Claude Code on the web. Operator framing was explicit: "Fire up and continue with a full chunk context session. Any questions for me put in question chips I'll answer for you." Carpenter immediately surfaced the open-joining substrate decision as a four-option AskUserQuestion chip (Option 1 org-roster / Option 2 verifier-walks-auth-tree / Option 3 hybrid / Defer); operator picked Option 3 hybrid as recommended. That single chip-tap locked Phase E3's scope. Session proceeded autonomously through cut 1 (policy evaluator + receiveSelfMembership gating) and cut 2 (open-member roster envelope + buffer) without further directional input needed.

## WHAT-CHANGED-RECENTLY

Two commits of substantive code on the dispatch branch plus one close-out flush. Cut 1 (commit 58af435) shipped the org-side join-policy evaluator at src/features/governance/evaluateJoinPolicy.ts — a pure function taking the JoinPolicy from Phase E1's discriminated-union shape, the incoming self-membership Attestation, and the org's holdings, returning {accepted, reason}. Three of six policy kinds (open / allow_list / deny_list) accept or reject purely from the envelope's subject leaf with case + whitespace normalization; the three proof-requiring kinds (requires_handshake / requires_credential / requires_vouch) reject with a reason naming Phase E4 as the milestone that adds joiner-side proof attachment. receiveSelfMembership in createMembership.ts grew from Phase E2 placeholder behavior (just holdAndAnchor) into a three-gate structural acceptor — envelope shape, org must hold its own self-declaration with a join rule, policy must accept the joiner. HomeScreen.acceptSelfMembership now passes orgDeclaration + holdings through; early-returns with console.warn when the wallet has not declared itself as an org. Cut 1 added 9 evaluateJoinPolicy tests + 3 receiveSelfMembership scenarios = 12 new tests (176 → 188).

Cut 2 (commit c6dcc07) shipped the open-member roster substrate at src/features/connections/openMemberRoster.ts. Same envelope-shape pattern publishOfficialsRoster uses: credential-kind attestation, subject = org pubkey, credential_type = 'open_member_roster', members leaf = canonical JSON of {member_id, joined_at, self_membership_envelope_id} sorted ascending by joined_at then member_id, issued_at at publish time. Six exports: acceptedSelfMemberships (all in holdings addressed to the org, sorted), pendingSelfMemberships (delta minus already-on-latest-roster), buildOpenMemberRosterDraft (pure unsigned builder), publishOpenMemberRoster (sign + hold + anchor pipeline), findLatestOpenMemberRoster (latest-by-issuedAt selection mirroring findLatestOfficialsRoster), readOpenMemberRoster (shape-tolerant member-list parser). 14 new tests (188 → 202).

Close-out flush will overwrite this file plus the four other artifacts (current.json, interactions.jsonl, in-flight.jsonl, carpenter-opinions.md) plus CARPENTER_HANDOFF.md, and push branch tip to main per PFOR-016.

## WHAT'S-PENDING

Nothing in-flight. Working tree is clean as of pre-close-out. Branch will be at the same SHA as main after the close-out push. Next session inherits a clean shared base with the Phase E3 substrate fully landed and Phase E4 (verifier + UI) as the natural next arc — about two-to-three sessions per the brief.

## WHAT-TO-FLAG

The receiveSelfMembership signature now requires orgSelfDecl as a parameter from the caller. HomeScreen.acceptSelfMembership does the lookup correctly via the existing orgDeclaration useMemo. Any new caller path (a ScanEnvelopeModal flow, a batch-import path, an AI-driven acceptor) needs to remember to do the lookup. A worthwhile chip next session: centralize the lookup inside receiveSelfMembership itself (take wallet + holdings, derive declaration via findOwnOrgDeclaration), or keep the caller-side pattern uniform with the rest of the create-attestation function family in this domain. Either choice is defensible; the question is consistency.

publishOpenMemberRoster hits anchorQueue.upsert which uses IndexedDB which jsdom does not back. Tests exercise the pure builders + signed wallet.hold directly so the same envelope shape the production path emits is verified, but the full publish-pipeline storage round-trip is not exercised. Same gap Phase E2's receiveSelfMembership tests carried forward; not blocking, just honest about the test surface. fake-indexeddb or a polyfill would close this if Phase E4 wants storage assertions on the publish path.

The bundle-budget script still buckets 11 unrecognized JS chunks under the catch-all unnamed-budget line. No change this session — openMemberRoster has no production caller yet so it doesn't form a chunk. Phase E4 will wire the publish button which means a new chunk forms, and that's when the named-budget debt becomes load-bearing. Worth doing the bundle-budget naming sweep before Phase E4 so the new chunk ships with a real budget.

createOrganization.ts (546 lines), governance/authRule.ts (430 lines), SettingsScreen.tsx (789 lines), HomeScreen.tsx (757 lines after this session), and several others remain in the soft-warn band. Same extraction plans apply as the prior letter named; none blocking. createMembership.ts grew to 251 lines this session — well under the 400 soft warn, room to add the Phase E4 UI helpers if they earn it.

The three-manifest governance-direction notes sweep is STILL pending. Fifth session deferred now. Low-energy autonomous cut, keeps slipping because each session takes a load-bearing axis. Worth picking up when a session opens without a substantive directional pull.

## RECOMMENDED-NEXT-MOVES

Primary: Phase E4 — verifier + UI. verifyOpenJoinedMembership composes findAuthRule (the Option 2 auth-tree walk) with findLatestOpenMemberRoster + readOpenMemberRoster (the Option 1 roster walk); under hybrid, accept whichever proof verifies. UI work: org creation form gains a membership-policy picker, org-mode Identity tab gains a Members view rendering the chronological roster (founder first by anchor height, joiners after), publish-roster button + pending-delta surface, any-wallet Profile gains a Join-an-org flow (paste / scan org pubkey → see declared join policy → click Join → self-membership signed + shipped). Brief estimates 2-3 sessions for the full Phase E4 arc.

Alternatives if Phase E4 is too big a swing: Phase D charter-amendment chain (walkCharterChain + findActiveCharter + dissolution endpoint, continues the org-control axis in parallel — does NOT depend on Phase E substrate); three-manifest governance-direction notes sweep (low-energy autonomous polish, FIFTH session deferred now); bundle-budget unnamed-chunk hygiene (11 chunks need named budgets, worth doing before Phase E4 wires a new chunk); createOrganization.ts officials-roster extraction (75 lines into a sibling, drops file under soft warn).

## OPERATOR'S-CURRENT-VIBE

Sharp and trusting, full-chunk delegation mode. Opening prompt was explicit about wanting maximum productive use of the context window and a preference for chip-form questions over prose escalation. The substrate-decision chip was the single most important pending decision; surfacing it immediately as the first move was the right call and the operator picked the recommended option without prose pushback. Mid-session there were no operator interventions, no redirections, no chip-form questions surfaced — the session ran autonomously through both cuts. Tonally the operator is in delegate-and-verify mode: trusts the carpenter to choose scope inside the locked substrate, expects the close-out narrative to be the operator-facing surface, and prefers the chip-form interface for any directional question that can be enumerated. Phase E4's first chip might be about how aggressive the verifier should be on the deferred-policy kinds (does it accept Phase E3 cut 1's "deferred to Phase E4" envelopes as valid once it learns to read proof attachments, or do we require a new envelope round-trip), but that's a Phase E4 concern, not a now concern.

## Ideas ready to revisit

Nothing surfaced this session that warrants new entries in ideas.md. The Living-Ideas doctrine triggers on new operator surfacing; this session was tight execution against a brief the operator had already approved, with one chip for the substrate decision and no other directional input. Two existing ideas worth gently surfacing if the next session has a quiet moment: the heartbeat-WIP-commit guard the prior letter named (catches dispatched sessions that die ungracefully with uncommitted work in the tree — would have saved the recovery cost two sessions ago); and the central-receiver-lookup pattern question this session surfaced in section 2 of opinions (does receiveSelfMembership do its own findOwnOrgDeclaration internally, or keep the caller-pass pattern uniform with the create-attestation family). Neither is urgent.
