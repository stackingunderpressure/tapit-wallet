# carpenter-state-for-foreman — Phase E4 cut 1 + bundle-budget hygiene + manifest sweep

> PFOR-012 structured operational state. Written 2026-05-26 late afternoon at session close-out. Three code commits plus one close-out commit landed on the dispatch branch `claude/multisig-orgs-status-jiLwm` this session. Branch tip pushed to main per PFOR-016 so the Stop-hook gate clears. All four gates green throughout every commit. 202 of 202 tests on session start → 221 of 221 on session end (+19, all from cut 1).

**Operator-mode note:** Human-driven session via Claude Code on the web. Operator opening was explicit full-chunk delegation: "Fire up and continue on fill up your session with as much context as you're comfortable taking on." Prior letter named Phase E4 cut 1 (the verifier function) as the recommended first move with the design fully composable from already-shipped substrate, so the session opened directly into that work without surfacing any chip-form questions. Cut 1 shipped autonomously; the remaining session budget was spent on the two architectural-polish items the prior letter named as worth landing BEFORE Phase E4 UI lands.

## WHAT-CHANGED-RECENTLY

Three substantive commits on the dispatch branch plus one close-out flush. Commit 05dff84 shipped Phase 8 Phase E4 cut 1 — the open-joined-membership verifier at src/features/connections/verifyOpenJoinedMembership.ts (200 lines) plus its test file (246 lines). The verifier is a pure composer over six already-shipped pieces: findAuthRule + isJoinRule from governance/authRule.ts (Phase E1), evaluateJoinPolicy from governance/evaluateJoinPolicy.ts (Phase E3 cut 1), isOpenMemberRoster + readOpenMemberRoster from connections/openMemberRoster.ts (Phase E3 cut 2), isSelfMembership from connections/createMembership.ts (Phase E2), isOrganizationSelfDeclaration from connections/createOrganization.ts (Phase 5b-org-i). The single export verifyOpenJoinedMembership(envelope, orgSelfDecl, currentRoster?) returns {valid, reason, proofPath} where proofPath names 'roster' on Option 1 success, 'auth_tree' on Option 2 success, or 'none' on rejection. Four structural gates run first; then the roster path is tried (cheaper, single artifact); then the auth-tree path falls back. 19 new tests cover all the matrix slices (4 structural + 9 auth-tree + 5 roster + 1 case-insensitivity).

Commit 1946cc8 shipped the bundle-budget unnamed-chunk hygiene sweep at scripts/bundle-budget.mjs. Eleven chunks that previously bucketed under the catch-all "(unrecognized js chunk — add a named budget)" line gained explicit named budgets with ~40-50% headroom above current gz size and an inline comment per chunk explaining what it carries and the growth direction. The catch-all line stays in place as the safety net for genuinely-new chunks but no longer fires for chunks that ship today. The 11 chunks: FreshComposeFAB, FreshCrew, FreshMemoriesStrip, FreshStreakIndicator, FreshTodayCarousel, QuickShareModal, ShareProofModal, createPresence helpers, normalizeImage helper, pendingOnboarding holder, webauthn helpers.

Commit 780bd50 shipped the three-manifest governance-direction notes sweep that the last five close-out letters named as worth doing. cosigning/manifest.ts gained a sentence explaining the Phase 8 Phase C cut 3 orgContext mode on CosignRequestModal. settings/manifest.ts gained a sentence explaining the Phase 8 Phase C cut 2 OrgRulesEditor + AppearanceSection Fresh extras. connections/manifest.ts gained an anchor framing sentence positioned between the credential-vs-handshake intro and the per-phase paragraphs naming the substrate-as-foundation dependency direction crisply.

Close-out flush will overwrite this file plus the four other artifacts (current.json, interactions.jsonl, in-flight.jsonl, carpenter-opinions.md) plus CARPENTER_HANDOFF.md, and push branch tip to main per PFOR-016.

## WHAT'S-PENDING

Nothing in-flight. Working tree is clean as of pre-close-out. Branch will be at the same SHA as main after the close-out push. Next session inherits a clean shared base with Phase E4 cut 1 (the verifier) landed and Phase E4 cuts 2-3 (proof-required policy extensions + UI surfaces) as the natural next arc.

## WHAT-TO-FLAG

Phase E4 cut 2 is going to be the next genuinely-directional moment. The three proof-required policy kinds (requires_handshake / requires_credential / requires_vouch) need joiner-side proof attachment to the self-membership envelope. The design choice on HOW proofs attach — full attestation versus disclosureProof bundle — is a chip-form question worth asking up front since the answer locks the shape of every future open-joined-with-proof envelope. Carpenter's recommendation in section 2 of carpenter-opinions.md is: disclosure-proof for credential and handshake (both already have disclosureProof-shaped reveal paths through tapit-attest), full-cosignature for vouch (already on the envelope's signatures field).

The connections manifest notes field is now around fourteen kilobytes of unbroken text covering ten phases of work. A markdown-shaped notes field with sub-sections would surface the structure better; the manifest type allows multi-line strings so the readability cost is real and the migration cost is one search-and-replace. Worth a clean-up cut some session when nothing more substantive is calling.

The receiveSelfMembership signature still requires orgSelfDecl as a parameter from the caller (the prior-prior letter named this). HomeScreen.acceptSelfMembership does the lookup correctly via the existing orgDeclaration useMemo, but any new caller path needs the same lookup. A worthwhile decision next session: centralize the lookup inside receiveSelfMembership itself (take wallet + holdings, derive declaration via findOwnOrgDeclaration), or keep the caller-side pattern uniform with the rest of the create-attestation function family. Either choice is defensible; the question is consistency.

publishOpenMemberRoster hits anchorQueue.upsert which uses IndexedDB which jsdom does not back. Tests exercise the pure builders + signed wallet.hold directly so the same envelope shape the production path emits is verified, but the full publish-pipeline storage round-trip is not exercised. Not blocking, just honest about the test surface. fake-indexeddb or a polyfill would close this if a future session wants storage assertions on the publish path.

createOrganization.ts (546 lines), governance/authRule.ts (430 lines), SettingsScreen.tsx (789 lines), HomeScreen.tsx (757 lines) all remain in the soft-warn band. The createOrganization.ts officials-roster extraction is the cleanest one — ~185 lines into an officialsRoster.ts sibling next to openMemberRoster.ts, drops createOrganization.ts from 546 to ~361 (well under the soft warn), and mirrors the openMemberRoster.ts file structure for a satisfying parallel. Carpenter considered taking it on as a fourth commit today but the session was already substantive and the extraction is the kind of move that benefits from a fresh session's full attention.

The bundle-budget catch-all no longer fires for currently-shipped chunks but it stays in place as the safety net for new ones. Phase E4 UI work (cuts 2-3) will form new chunks (membership-policy picker, Members view, publish-roster button, any-wallet join-an-org flow) — when those land they should emerge under explicit named budgets in scripts/bundle-budget.mjs alongside the existing entries rather than under the catch-all again.

## RECOMMENDED-NEXT-MOVES

Primary: Phase E4 cut 2 — extend evaluateJoinPolicy to read proof-required policy kinds. Suggest opening the session with a chip-form question to lock the proof-attachment shape (full attestation vs disclosureProof bundle) before any code lands; the recommended option per carpenter-opinions.md section 2 is disclosure-proof for credential and handshake, full-cosignature for vouch. Joiner-side: buildSelfMembershipDraft gains optional proof-attachment parameters; envelope grows new leaves carrying canonical-JSON proof bundles. Org-side: evaluateJoinPolicy reads the leaves and verifies them. verifyOpenJoinedMembership public signature stays stable — cut 2 work is internal to the evaluator. One session for design + the first policy kind (probably requires_credential since it's the cleanest); the remaining two kinds can slot into the same architecture in follow-on cuts. After cut 2 lands, cut 3 wires the UI.

Alternatives if Phase E4 cut 2 is too design-heavy for the next session: Phase D charter-amendment chain (walkCharterChain + findActiveCharter + dissolution endpoint, continues the org-control axis in parallel — does NOT depend on Phase E substrate); createOrganization.ts officials-roster extraction (~185 lines into an officialsRoster.ts sibling, drops file under soft warn, mirrors openMemberRoster.ts structure); connections manifest notes refactor (markdown sub-sections instead of one long prose blob, lowers cognitive load for future readers).

## OPERATOR'S-CURRENT-VIBE

Same delegate-and-verify mode the prior session ran under. Opening prompt was explicit about wanting maximum productive use of the context window. Prior letter named the substantive first move (Phase E4 cut 1) so no opening chip was needed; carpenter proceeded autonomously. After cut 1 landed with ample context remaining, carpenter pivoted to two architectural-polish items the prior letter explicitly named as worth doing — bundle-budget hygiene and the five-session-deferred manifest sweep — and shipped both. No mid-session operator interventions, no redirections, no chip-form questions surfaced. The session was three substantive commits, none of which required directional input from the operator beyond the opening permission to fill the context window. Phase E4 cut 2 IS the next chip-form question — operator should expect to chip the proof-attachment shape at session start (the recommendation is in the carpenter-opinions section 2). After that, the cut 2 implementation runs autonomously.

## Ideas ready to revisit

Nothing new surfaced this session. The Living-Ideas doctrine triggers on operator surfacing; this session was tight autonomous execution with no operator input beyond the opening framing. Two existing items worth gently surfacing if the next session has a quiet moment: the connections manifest notes refactor to markdown sub-sections (carpenter-opinions section 2 named this); the heartbeat-WIP-commit guard the prior-prior letter named (catches dispatched sessions that die ungracefully with uncommitted work). Neither is urgent.
